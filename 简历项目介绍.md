# TechSpar — AI 面试训练系统 | 简历项目介绍

---

## 一、简历推荐写法（精简版，适合放简历项目经历栏）

### 项目名称：TechSpar — AI 驱动的自适应面试训练系统

**技术栈：** FastAPI / React 19 / LangGraph / LlamaIndex / SQLite / SSE / Docker

**项目描述：**
基于大语言模型构建的智能面试训练平台，支持简历模拟面试、专题刷题、JD 岗位针对性训练、录音分析等多模式训练。系统通过持久化用户画像 + 间隔重复算法（SM-2）持续追踪用户薄弱点与进步轨迹，实现个性化出题与自适应难度调整。

**核心工作与亮点：**

- **基于 LangGraph 的多阶段面试状态机**：设计了 greeting → self_intro → technical → project_deep_dive → reverse_qa → end 六阶段面试流程，利用 LangGraph 的 `interrupt_before` + `MemorySaver` 实现有状态的多轮对话中断恢复，支持面试官动态追问与阶段智能推进
- **Mem0 风格的用户画像记忆系统**：设计两阶段 LLM 更新流程（Extract → Merge），通过向量嵌入去重（余弦相似度 ≥ 0.75）+ 14 天半衰期时间衰减函数，实现薄弱点的语义去重与时效性管理
- **SM-2 间隔重复调度引擎**：将面试评分映射到 SM-2 质量分，驱动薄弱知识点的间隔复习调度（1d → 3d → 7d → 21d...），使刷题优先覆盖到期复习点
- **RAG 知识增强出题**：基于 LlamaIndex 构建知识库 + 简历的向量索引，面试出题时检索相关上下文（Top-K），确保问题与候选人背景高度相关
- **题目关系图谱可视化**：对历史题目进行嵌入编码，基于余弦相似度（≥ 0.65）构建知识图谱，使用 react-force-graph-2d 力导向图展示题目聚类关系
- **SSE 流式对话 + 双写会话持久化**：AI 助手采用 Server-Sent Events 流式输出；面试会话采用内存缓存 + SQLite 双写模式，兼顾实时性与容灾恢复
- **多用户数据隔离**：JWT 认证 + 行级数据隔离 + 用户级知识库副本，确保多租户场景下的数据安全

---

## 二、详细版项目介绍（适合面试口述 / 项目详细说明）

### 2.1 项目概述

TechSpar 是一个 AI 驱动的面试训练系统。与传统一次性面试工具不同，它会持续记住用户的薄弱点和进步轨迹，通过间隔重复算法智能安排复习，实现"越练越精准"的个性化训练体验。

### 2.2 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (React 19)                   │
│  React Router v7 · Tailwind CSS v4 · Radix UI · Vite   │
│  Code Splitting · SSE Stream · Voice Input · Graph Viz  │
└──────────────────────┬──────────────────────────────────┘
                       │ REST API / SSE
┌──────────────────────▼──────────────────────────────────┐
│                   Backend (FastAPI)                       │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │  LangGraph   │  │  LlamaIndex  │  │  SM-2 Engine  │  │
│  │  状态机面试  │  │  RAG 知识检索 │  │  间隔重复调度 │  │
│  └─────────────┘  └──────────────┘  └───────────────┘  │
│                                                          │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │ 用户画像系统 │  │  向量记忆层   │  │  会话管理层   │  │
│  │ (Mem0 风格) │  │ (Embedding)  │  │ (双写持久化) │  │
│  └─────────────┘  └──────────────┘  └───────────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│            Storage Layer                                 │
│  SQLite(WAL) · JSON Profile · Vector BLOB · File System │
└─────────────────────────────────────────────────────────┘
```

### 2.3 核心技术亮点详解

#### 亮点一：LangGraph 多阶段面试状态机

**问题**：传统 Chatbot 无法实现有结构的面试流程（分阶段、有追问、可中断恢复）。

**方案**：基于 LangGraph 构建面试状态机，定义六个面试阶段，每个阶段有独立的出题策略和转场条件。

```
START → init(加载简历) → wait(等待用户输入)
           ↓
       ask(提问) → wait → route_after_answer(条件路由)
                            ├─ ask（继续追问）
                            ├─ advance（推进到下一阶段）
                            └─ end（面试结束）
```

**关键技术点：**
- 利用 `interrupt_before=["wait"]` 在等待用户输入时暂停图执行，避免阻塞
- 使用 `MemorySaver` 做状态检查点，支持对话中断后恢复
- LLM 在回复中内嵌隐式评估 `<!--EVAL:{...}-->` JSON，实现实时评分 + 路由决策
- 每阶段最多 5 个问题，路由节点根据评估结果决定追问/推进/结束

#### 亮点二：Mem0 风格用户画像记忆系统

**问题**：如何让系统"记住"用户跨会话的能力表现，且避免信息冗余和过时？

**方案**：设计两阶段 LLM 更新流程 + 向量去重 + 时间衰减。

```
面试结束 → Extract（提取摘要）→ LLM 分析 → 结构化输出
    ↓
Merge（合并画像）→ 对每个事实执行 ADD/UPDATE/NOOP/IMPROVE 操作
    ↓
向量去重（余弦 ≥ 0.75 判定重复）→ 时间衰减（14天半衰期）→ 持久化
```

**画像数据结构：**
- `topic_mastery`：各主题掌握度（1-5 级）
- `weak_points`：薄弱点列表（含 SM-2 调度状态、首次/末次出现时间、改善标记）
- `strong_points`：优势领域
- `communication`：沟通风格分析
- `thinking_patterns`：思维模式（结构化、深度、追问应对）
- `stats`：训练统计（总场次、平均分、趋势）

#### 亮点三：SM-2 间隔重复调度

**问题**：用户薄弱点很多，如何科学安排复习优先级？

**方案**：引入 SuperMemo SM-2 算法，将面试评分（0-10）映射为 SM-2 质量分（0-5），驱动复习调度。

- 通过（质量 ≥ 3）：间隔递增 1d → 3d → 7d → ease_factor * interval
- 未通过（质量 < 3）：重置到 1 天
- ease_factor 动态调整（范围 1.3 ~ 5.0）
- 刷题出题时优先抽取 `next_review ≤ today` 的薄弱点

#### 亮点四：RAG 知识增强出题

**问题**：纯 LLM 出题容易脱离实际知识体系，产生幻觉。

**方案**：基于 LlamaIndex 构建双索引（知识库 + 简历），出题时检索 Top-K 相关文档片段注入 Prompt。

- 支持 8 大知识域（Python/Java/Spring/数据库/系统设计/计算机基础/算法/AI大模型）
- 知识库以 Markdown 文件存储，用户可自定义（每用户独立副本）
- 索引缓存三级策略：内存字典 → 磁盘持久化 → 按需重建
- 索引失效：文件更新时自动清除缓存触发重建

#### 亮点五：会话管理 — 双写持久化模式

**问题**：LangGraph 图实例不可序列化，服务重启会丢失进行中的面试。

**方案**：内存缓存 + SQLite 双写，读取时内存优先、数据库兜底。

```python
# 写入：同时写内存和 DB
store[session_id] = data          # 内存（快速读取）
save_live_session(...)            # SQLite（持久化兜底）

# 读取：内存优先，DB 回退
data = store.get(session_id) or load_live_session(session_id)
```

- 算法会话支持 LangChain Message 对象的序列化/反序列化
- 24 小时自动清理过期会话
- 三种会话类型独立管理：resume（图实例）、drill（批量题目）、algorithm（多轮对话）

#### 亮点六：前端工程化实践

- **React 19 + React Router v7**：11 个页面全量 `React.lazy()` 代码分割，首屏加载优化
- **SSE 流式渲染**：AI 助手采用 `ReadableStream` + 异步生成器逐 token 渲染
- **语音输入双后端**：优先使用浏览器原生 Web Speech API，降级为服务端 ASR（DashScope）
- **力导向图可视化**：基于 react-force-graph-2d，自定义 Canvas 绘制节点，按分数着色
- **自适应主题系统**：60+ CSS 变量 + 明暗主题切换，MD3 风格设计语言

### 2.4 技术难点与解决方案汇总

| 技术难点 | 解决方案 |
|---------|---------|
| 多阶段面试流程控制 | LangGraph 状态机 + interrupt + MemorySaver |
| 用户画像跨会话持久化 | Mem0 风格两阶段 LLM 更新 + JSON 存储 |
| 薄弱点语义去重 | Embedding 余弦相似度 ≥ 0.75 判定重复 |
| 画像信息时效性 | 14 天半衰期指数衰减函数 |
| 复习优先级调度 | SM-2 间隔重复算法 |
| LLM 出题质量保障 | LlamaIndex RAG 检索增强 |
| 面试会话容灾 | 内存 + SQLite 双写模式 |
| LLM 输出解析容错 | 三级 JSON 解析（直接 → Markdown 块 → 首个括号） |
| 多用户数据安全 | JWT + 行级隔离 + 用户级知识库副本 |
| 首屏性能优化 | React.lazy 代码分割 + 索引三级缓存 |

### 2.5 项目数据（可选填充）

- 覆盖 8 大技术知识域，支持 4 种训练模式
- 后端 15+ API 模块，40+ RESTful 接口
- 前端 13 个路由页面，15+ 功能组件
- 支持 Docker 一键部署
