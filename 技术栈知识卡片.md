# TechSpar 技术栈知识卡片集

> 每张卡片 = 一个技术点。格式：**是什么 → 核心概念 → 在本项目怎么用 → 面试记忆点**

---

# 一、后端框架层

---

## Card 01 | FastAPI

**一句话：** Python 的高性能异步 Web 框架，基于 Starlette（ASGI）+ Pydantic 构建。

**核心概念：**
```
ASGI（异步服务器网关接口）
  ↕ 对比
WSGI（同步，Flask/Django 默认）

关键特性：
├── async/await 原生异步路由
├── Pydantic 自动请求校验 + 序列化
├── Depends() 依赖注入系统
├── 自动生成 OpenAPI 文档（/docs）
└── StreamingResponse 支持 SSE
```

**在本项目中：**
- 全部 40+ API 接口的承载框架
- `Depends(get_current_user)` 实现认证守卫
- `StreamingResponse` 驱动 AI 助手的 SSE 流式输出
- Pydantic 模型校验面试请求参数

**面试记忆点：**
- FastAPI 底层是 Starlette（ASGI），不是 Flask（WSGI）
- `async def` 路由在事件循环中执行；普通 `def` 路由自动放入线程池
- 依赖注入是树形的——依赖可以依赖其他依赖

---

## Card 02 | Pydantic

**一句话：** Python 的数据校验和设置管理库，用类型注解定义数据模型。

**核心概念：**
```python
from pydantic import BaseModel

class StartInterviewRequest(BaseModel):
    mode: str                    # 必填
    topic: str | None = None     # 可选，默认 None

# 自动校验：传入 {"mode": 123} → 报错（期望 str）
# 自动转换：传入 {"mode": "resume", "topic": "python"} → 类型安全的对象
```

**在本项目中：**
- `models.py` 定义了所有 API 的请求/响应模型
- `pydantic-settings` 管理 `.env` 配置（`config.py` 中的 `Settings` 类）
- FastAPI 路由参数自动绑定 Pydantic 模型

**面试记忆点：**
- Pydantic V2 用 Rust 重写核心，性能提升 5-50 倍
- `BaseModel` 用于 API 数据；`BaseSettings` 用于环境变量/配置
- 与 `TypedDict` 的区别：Pydantic 有运行时校验，TypedDict 只是类型提示

---

## Card 03 | Uvicorn

**一句话：** Python 的 ASGI 服务器，FastAPI 应用的实际运行容器。

**核心概念：**
```
请求流程：
客户端 → Nginx → Uvicorn（ASGI Server）→ FastAPI（ASGI App）

类比：
Uvicorn 之于 FastAPI ≈ Gunicorn 之于 Flask
（但 Uvicorn 是异步的，Gunicorn 是同步的）
```

**在本项目中：**
```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
#       模块路径:app对象   绑定地址       端口       热重载（开发用）
```

**面试记忆点：**
- 生产环境通常用 `gunicorn -k uvicorn.workers.UvicornWorker` 多进程部署
- `--reload` 只用于开发，生产禁用（性能开销大）
- Uvicorn 基于 `uvloop`（libuv 的 Python 绑定），性能优于默认 asyncio 事件循环

---

# 二、AI / LLM 编排层

---

## Card 04 | LangChain

**一句话：** LLM 应用开发框架，提供 Prompt 模板、链式调用、消息抽象、工具集成等基础设施。

**核心概念：**
```
核心抽象：
├── ChatModel      → LLM 调用封装（支持 OpenAI / Claude / 本地模型）
├── Message 类型   → HumanMessage / AIMessage / SystemMessage / ToolMessage
├── Prompt 模板    → ChatPromptTemplate，变量注入
├── Tool           → 函数 → LLM 可调用的工具
└── Memory         → 对话历史管理（本项目未用，自己管理）

调用方式：
llm = ChatOpenAI(model="gpt-4", api_base="...", api_key="...")
response = llm.invoke([SystemMessage("你是面试官"), HumanMessage("你好")])
# response = AIMessage(content="你好，请先自我介绍...")
```

**在本项目中：**
- `llm_provider.py` 初始化 OpenAI 兼容的 ChatModel
- Message 对象贯穿所有对话（面试、助手、算法求解）
- AI 助手使用 Tool 机制调用用户数据查询工具
- `astream()` 异步流式输出用于 SSE

**面试记忆点：**
- LangChain 是**胶水层**，核心价值是统一不同 LLM 提供商的接口
- Message 类型不是纯 dict，序列化/反序列化需要特殊处理（本项目踩过坑）
- `invoke()` 是同步，`ainvoke()` 是异步，`stream()` / `astream()` 是流式

---

## Card 05 | LangGraph

**一句话：** LangChain 团队推出的**有状态、可中断的图执行引擎**，用于构建多步骤 Agent 和工作流。

**核心概念：**
```
图的构成：
├── State（状态）   → TypedDict，定义图的全局数据
├── Node（节点）    → 函数，执行具体动作（如 ask、wait、route）
├── Edge（边）      → 节点间的转移关系
├── Conditional Edge → 条件路由（根据状态决定下一步）
├── Checkpointer    → 状态持久化（MemorySaver / RedisSaver）
└── interrupt_before → 在指定节点前暂停，等待外部输入

执行模型：
graph.invoke(input)   → 执行到终点或中断点
graph.get_state()     → 获取当前状态
graph.update_state()  → 外部注入数据（如用户输入）
graph.invoke(None)    → 从中断点恢复执行
```

**在本项目中：**
- `graphs/resume_interview.py`：六阶段面试状态机（核心）
- `graphs/topic_drill.py`：专题刷题流程
- `graphs/job_prep.py`：JD 岗位训练流程
- `interrupt_before=["wait"]` 实现异步等待用户输入
- `MemorySaver` 做内存级状态检查点

**面试记忆点：**
- LangGraph vs LangChain Chain：Chain 是线性的，Graph 支持分支、循环、条件路由
- `interrupt_before` 是实现"人在环中"(Human-in-the-loop) 的关键机制
- MemorySaver 是内存级的（重启丢失），生产应换 SqliteSaver / RedisSaver
- State 中的 `messages` 用 `Annotated[list, add_messages]` 注解，实现消息自动追加而非覆盖

---

## Card 06 | LlamaIndex

**一句话：** 专注于 RAG（检索增强生成）的框架，将私有数据连接到 LLM。

**核心概念：**
```
RAG 流程：
文档 → 分片(Chunking) → 向量化(Embedding) → 存入索引(Index)
                                                    ↓
用户查询 → 查询向量化 → 相似度检索(Top-K) → 上下文注入 Prompt → LLM 生成

LlamaIndex 核心类：
├── SimpleDirectoryReader  → 读取目录下的文件（.md/.txt/.pdf）
├── VectorStoreIndex       → 向量索引（默认内存存储）
├── StorageContext         → 索引持久化到磁盘
├── QueryEngine            → 封装检索+生成的完整流程
└── ServiceContext         → 配置 LLM、嵌入模型、分片策略
```

**在本项目中：**
- `indexer.py`：构建知识库索引和简历索引
- 知识库 8 大技术领域，Markdown 文件 → 向量索引
- 简历 PDF → 向量索引，面试出题时检索候选人经历
- 三级缓存：内存 dict → 磁盘 persist → 源文件重建

**面试记忆点：**
- LlamaIndex 侧重**数据检索**，LangChain 侧重**LLM 编排**，两者互补
- `VectorStoreIndex.from_documents()` 是最常用的索引构建方式
- `index.as_query_engine(similarity_top_k=5)` 创建查询引擎
- 分片大小影响检索质量：太大上下文冗余，太小丢失语义

---

## Card 07 | Embedding（向量嵌入）

**一句话：** 将文本映射为固定维度的稠密向量，使语义相近的文本在向量空间中距离更近。

**核心概念：**
```
"Python GIL 是什么" → [0.12, -0.34, 0.78, ...] (384维向量)
"全局解释器锁原理" → [0.11, -0.32, 0.80, ...] (语义相近 → 向量相近)
"今天天气好"       → [0.95, 0.21, -0.56, ...] (语义不同 → 向量远离)

相似度度量：
余弦相似度 = dot(A, B) / (||A|| * ||B||)
范围：[-1, 1]，越接近 1 越相似

常见模型：
├── OpenAI text-embedding-3-small (1536维)
├── BAAI/bge-m3 (1024维，多语言，本项目默认)
└── sentence-transformers 系列
```

**在本项目中：**
- RAG 检索：知识库文档 + 简历 → 向量化 → 相似度匹配
- 薄弱点去重：新提取 vs 已有薄弱点，余弦 ≥ 0.75 判重复
- 题目图谱：题目 → 向量 → 两两相似度 ≥ 0.65 建边
- 记忆搜索：历史面试洞察 → 向量化 → 语义搜索

**面试记忆点：**
- Embedding 是**单向映射**（文本→向量），不能从向量还原文本
- 维度越高信息越丰富，但存储和计算成本也越高
- 批量嵌入 `get_text_embedding_batch()` 比逐条调用高效得多
- 本项目用 SQLite BLOB 存向量（`np.float32.tobytes()`），无需外部向量数据库

---

## Card 08 | RAG（检索增强生成）

**一句话：** 先从知识库检索相关文档，再将检索结果注入 LLM Prompt，使生成内容有据可依。

**核心概念：**
```
为什么需要 RAG？
├── LLM 知识有截止日期（不知道最新信息）
├── LLM 不知道私有数据（你的简历、你的知识库）
├── LLM 会幻觉（编造不存在的 API / 概念）
└── RAG = 给 LLM 开卷考试

RAG 三步走：
1. Index（离线）：文档 → 分片 → 嵌入 → 存储
2. Retrieve（在线）：查询 → 嵌入 → Top-K 相似片段
3. Generate（在线）：系统Prompt + 检索片段 + 用户问题 → LLM 生成

关键参数：
├── Chunk Size：分片大小（影响检索粒度）
├── Top-K：返回前 K 个最相关片段
├── Similarity Threshold：最低相似度阈值
└── Overlap：分片重叠区域（保持上下文连贯）
```

**在本项目中：**
```python
# 出题 Prompt 示例
system_prompt = f"""你是面试官，基于以下知识内容出题：

## 相关知识（RAG 检索结果）
{rag_context}        # ← Top-5 检索片段

## 候选人简历
{resume_context}     # ← Top-3 简历片段

请出 10 道面试题..."""
```

**面试记忆点：**
- RAG 不改变 LLM 参数（vs Fine-tuning 改变参数）
- 检索质量 > 生成质量——检索错了，LLM 再强也没用
- Top-K 不是越大越好——注入太多上下文会稀释 LLM 注意力
- 本项目 Top-K：知识库用 5，简历用 3（简历信息密度高）

---

# 三、数据存储层

---

## Card 09 | SQLite + WAL 模式

**一句话：** 嵌入式关系数据库，单文件存储，零配置零运维。WAL 模式允许读写并发。

**核心概念：**
```
SQLite 锁机制：
├── DELETE 模式（默认）：写时全库锁，读写互斥
└── WAL 模式：Write-Ahead Log，读写可并发
     ├── 写操作追加到 WAL 文件
     ├── 读操作读主数据库文件（不阻塞）
     ├── Checkpoint 时将 WAL 合并回主文件
     └── 限制：同时只允许一个写事务

PRAGMA 调优：
PRAGMA journal_mode = WAL;        -- 启用 WAL
PRAGMA synchronous = NORMAL;      -- 平衡安全/性能
PRAGMA foreign_keys = ON;         -- 启用外键约束
```

**在本项目中：**
- `interviews.db` 存储所有结构化数据（sessions, users, favorites, vectors...）
- WAL 模式确保面试进行时的数据库读写不互相阻塞
- `aiosqlite` 异步驱动，配合 FastAPI 的异步路由

**面试记忆点：**
- SQLite 适合场景：嵌入式/单机/读多写少/数据量 < 百万级
- WAL 模式的"多读一写"是理解本项目并发模型的关键
- `aiosqlite` 本质是将 sqlite3 操作放入线程池执行，并非真正的异步 I/O
- 连接不要跨协程共享——每次操作 `async with connect()` 新建连接

---

## Card 10 | JWT（JSON Web Token）

**一句话：** 无状态的认证令牌，服务端不存会话，令牌本身携带用户信息。

**核心概念：**
```
JWT 结构（三段式）：
Header.Payload.Signature

Header:  {"alg": "HS256", "typ": "JWT"}
Payload: {"user_id": "abc", "exp": 1700000000}
Signature: HMAC-SHA256(base64(Header) + "." + base64(Payload), SECRET)

验证流程：
1. 客户端发送 Authorization: Bearer <token>
2. 服务端用 SECRET 验证签名（防篡改）
3. 检查 exp 是否过期
4. 提取 payload 中的 user_id

对比 Session：
├── Session：状态存服务端（Redis/DB），客户端存 session_id
└── JWT：状态存客户端（token 自包含），服务端无状态
     ├── 优点：无需服务端存储，天然支持分布式
     └── 缺点：无法主动失效（除非加黑名单）
```

**在本项目中：**
- HS256 算法 + 7 天过期
- `bcrypt` 哈希密码（非明文/MD5）
- `Depends(get_current_user)` 在每个受保护路由解码 token 提取 user_id

**面试记忆点：**
- HS256 是对称加密（一个密钥），RS256 是非对称（公钥验证、私钥签名）
- JWT 不能存敏感信息——Payload 是 Base64 编码不是加密，任何人可解码
- 本项目无 Refresh Token——7 天后需重新登录。生产环境应加 Refresh Token 机制

---

## Card 11 | bcrypt

**一句话：** 专为密码设计的慢哈希算法，内置盐值，抗暴力破解。

**核心概念：**
```
MD5("password")        → 固定值，彩虹表可破
SHA256("password")     → 固定值，速度太快可暴力
bcrypt("password")     → 每次不同（自带随机盐），故意慢（cost factor）

bcrypt 流程：
hash = bcrypt.hashpw("password".encode(), bcrypt.gensalt())
# → b'$2b$12$LJ3m4ys2Y...'
#       ↑  ↑
#      版本 cost(2^12次迭代)

验证：
bcrypt.checkpw("password".encode(), stored_hash)
# 恒定时间比较，防止时序攻击
```

**在本项目中：**
- 注册时 `bcrypt.hashpw(password, bcrypt.gensalt())` 存库
- 登录时 `bcrypt.checkpw(input, stored_hash)` 验证

**面试记忆点：**
- bcrypt 的 cost factor 默认 12（2^12 = 4096 次迭代），可调高增加安全性
- 盐值内嵌在哈希结果中，不需要单独存储
- `checkpw` 是恒定时间比较——无论密码对错，耗时相同，防止通过响应时间猜测

---

# 四、前端技术层

---

## Card 12 | React 19 核心概念

**一句话：** 声明式 UI 库，组件化开发，虚拟 DOM 驱动高效更新。

**核心概念：**
```
本项目用到的 React 特性：
├── useState        → 组件状态管理（面试消息、表单输入）
├── useEffect       → 副作用处理（API 调用、自动滚动、定时器）
├── useRef          → DOM 引用（滚动容器、force-graph 实例）
├── useContext      → 跨组件共享状态（AuthContext）
├── useMemo         → 缓存计算结果（JD 分析签名比较）
├── lazy()          → 代码分割（11 个页面按需加载）
├── Suspense        → 懒加载占位（显示 Loading）
└── useCallback     → 缓存函数引用（防止子组件重渲染）
```

**在本项目中：**
- 纯 Hooks 函数组件，无 Class 组件（除 ErrorBoundary）
- `useState` 管理所有 UI 状态（无 Redux/Zustand）
- `useEffect` 驱动 API 调用和页面初始化
- `React.lazy` + `Suspense` 实现 13 个路由页面的代码分割

**面试记忆点：**
- React 19 新特性：`use()` hook、Server Components（本项目未用）
- ErrorBoundary 必须用 Class 组件（Hooks 不支持 `componentDidCatch`）
- `useEffect` 依赖数组为 `[]` 时 = componentDidMount，无依赖 = 每次渲染都执行

---

## Card 13 | React Router v7

**一句话：** React 官方路由库，支持嵌套路由、动态参数、路由守卫。

**核心概念：**
```
本项目路由结构：
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Home />} />           {/* 公开 */}
    <Route path="/login" element={<Login />} />      {/* 公开 */}
    <Route element={<ProtectedRoute />}>             {/* 守卫 */}
      <Route element={<AppShell />}>                 {/* 布局 */}
        <Route path="/interview/:sessionId" ... />   {/* 动态参数 */}
        <Route path="/review/:sessionId" ... />
        <Route path="/profile" ... />
        <Route path="*" element={<NotFound />} />    {/* 404 */}
      </Route>
    </Route>
  </Routes>
</BrowserRouter>

关键 API：
├── useNavigate()     → 编程式导航
├── useParams()       → 获取 URL 参数（:sessionId）
├── useLocation()     → 获取当前路径
├── <Outlet />        → 嵌套路由出口（AppShell 中渲染子路由）
└── <Navigate />      → 声明式重定向
```

**在本项目中：**
- 嵌套路由：`ProtectedRoute → AppShell → 具体页面`
- `ProtectedRoute` 检查 token，未登录重定向到 `/`
- `:sessionId` 动态参数用于面试和复盘页面

**面试记忆点：**
- v7 相比 v6：更好的类型安全、loader/action 数据加载（本项目未用 loader）
- `<Outlet />` 是嵌套路由的核心——父路由渲染 Outlet，子路由填入其中
- BrowserRouter 需要服务端配合（Nginx `try_files` 回退到 index.html）

---

## Card 14 | Vite

**一句话：** 下一代前端构建工具，开发时用 ESM 原生模块加载（极快），生产用 Rollup 打包。

**核心概念：**
```
Vite vs Webpack：
├── 开发启动：Vite 毫秒级（无需打包）vs Webpack 秒级（全量打包）
├── 热更新：Vite 只更新改动模块 vs Webpack 重新打包受影响的 chunk
├── 原理：Vite 利用浏览器原生 ES Module import
└── 生产：Vite 用 Rollup 打包（tree-shaking、代码分割、压缩）

关键配置：
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {           // 开发代理（解决跨域）
      "/api": { target: "http://localhost:8000" }
    }
  }
})
```

**在本项目中：**
- 开发服务器 `localhost:5173`，代理 `/api` 到后端 `localhost:8000`
- 生产构建产出 `dist/` 目录，由 Nginx 提供服务
- `@vitejs/plugin-react` 支持 JSX 和 Fast Refresh
- Tailwind CSS v4 通过 Vite 插件集成

**面试记忆点：**
- Vite 开发时不打包——浏览器直接请求 `.jsx` 文件，Vite 按需转译
- 开发代理只在 `npm run dev` 时生效，生产环境需要 Nginx 配置反向代理
- `npm run build` 产出的文件名含 hash（`index-a1b2c3.js`），天然支持长期缓存

---

## Card 15 | Tailwind CSS v4

**一句话：** 原子化 CSS 框架，用预定义的工具类直接在 HTML 中写样式，无需写 CSS 文件。

**核心概念：**
```
传统 CSS：
.card { padding: 16px; border-radius: 8px; background: white; }

Tailwind：
<div class="p-4 rounded-lg bg-white">

优势：
├── 无命名冲突（不写 class name）
├── 无死代码（JIT 只生成用到的样式）
├── 一致性（设计 token 内置：spacing、color、font...）
└── 响应式：md:flex-row（768px 以上横排）

v4 新特性：
├── 原生 CSS @theme 替代 tailwind.config.js
├── 自动内容检测（无需配置 content 路径）
└── 更快的构建速度（用 Rust 重写的引擎 Lightning CSS）
```

**在本项目中：**
- 全部样式用 Tailwind 工具类编写
- 60+ CSS 变量定义主题色（明暗主题切换）
- `clsx` + `tailwind-merge` 处理条件样式和类名冲突
- 30+ 自定义 `@keyframes` 动画（猫咪动画、脉冲点、飘浮等）

**面试记忆点：**
- Tailwind 不是组件库，是**工具类生成器**——它生成 `.p-4 { padding: 1rem }` 这样的原子类
- JIT（Just-In-Time）模式：只生成页面中实际用到的类，产出 CSS 极小
- `tailwind-merge` 解决 `"p-4 p-2"` 的冲突问题——后者覆盖前者

---

## Card 16 | Radix UI

**一句话：** 无样式（Headless）React 组件库，只提供行为和无障碍支持，样式完全自定义。

**核心概念：**
```
Radix UI vs Material UI / Ant Design：
├── Material UI：预设样式 + 行为（开箱即用，定制受限）
└── Radix UI：只有行为 + 无障碍，无预设样式（完全自定义外观）

本项目用到的 Radix 原语：
├── Dialog       → 模态对话框（确认删除、编辑表单）
├── DropdownMenu → 下拉菜单（更多操作）
├── Avatar       → 用户头像
├── Tabs         → 标签页切换
├── Tooltip      → 悬浮提示
├── Progress     → 进度条
├── ScrollArea   → 自定义滚动条
└── Separator    → 分隔线
```

**在本项目中：**
- `components/ui/` 目录下基于 Radix 封装了 9 个 UI 组件（shadcn 风格）
- 用 Tailwind 给 Radix 原语加样式
- 保证了键盘导航和屏幕阅读器兼容性

**面试记忆点：**
- Headless UI 的核心价值：**关注点分离**——行为和样式解耦
- shadcn/ui 就是 "Radix UI + Tailwind" 的封装范式（本项目同理）
- Radix 每个组件都处理了焦点管理、键盘交互、ARIA 属性

---

# 五、实时通信层

---

## Card 17 | SSE（Server-Sent Events）

**一句话：** 基于 HTTP 的单向服务端推送协议，服务端持续发送事件流到客户端。

**核心概念：**
```
SSE vs WebSocket：
├── SSE：单向（服务端→客户端），HTTP 协议，自动重连
└── WebSocket：双向，ws:// 协议，需手动重连

SSE 数据格式：
data: {"token": "你"}\n\n
data: {"token": "好"}\n\n
data: [DONE]\n\n

关键 HTTP 头：
Content-Type: text/event-stream
Cache-Control: no-cache
X-Accel-Buffering: no           ← Nginx 禁用缓冲（重要！）

前端消费方式：
├── EventSource API（只支持 GET）
└── fetch + ReadableStream（支持 POST，本项目选择）
```

**在本项目中：**
- AI 助手 `/api/assistant/chat` 使用 SSE 流式输出回复
- FastAPI `StreamingResponse` + `async generator` 产出事件
- 前端 `getReader()` + `TextDecoder` 解析流

**面试记忆点：**
- SSE 基于 HTTP/1.1，**不需要 WebSocket 升级**，代理兼容性更好
- 本项目用 `fetch` 而非 `EventSource`，因为需要 POST 请求 + 自定义 Header
- Nginx 代理 SSE 必须加 `proxy_buffering off`，否则数据会被缓冲到一起发送

---

# 六、算法与数据结构层

---

## Card 18 | SM-2 间隔重复算法

**一句话：** SuperMemo 2 算法，根据记忆质量动态调整复习间隔，实现高效长期记忆。

**核心概念：**
```
三个状态变量：
├── interval（天）：下次复习间隔
├── ease_factor：难度因子（初始 2.5，范围 1.3~5.0）
└── repetitions：连续正确次数

更新规则（quality 为 0-5 的质量分）：
if quality >= 3 (通过):
    rep=0 → interval=1
    rep=1 → interval=3
    rep≥2 → interval *= ease_factor
    repetitions += 1
else (未通过):
    interval = 1
    repetitions = 0

ease_factor 调整：
EF += 0.1 - (5-q) * (0.08 + (5-q) * 0.02)
EF = max(1.3, EF)

间隔增长示例（持续通过）：
1d → 3d → 7d → 17d → 43d → 107d → ...
```

**在本项目中：**
- 面试评分 0-10 → 除以 2 → SM-2 quality 0-5
- 每个薄弱点内嵌 SM-2 状态（interval, ease_factor, repetitions, next_review）
- 刷题出题时优先查询 `next_review <= today` 的薄弱点

**面试记忆点：**
- SM-2 是 1987 年的算法，简单但有效，Anki 的核心算法
- ease_factor 越低说明知识点越难（每次错误都会降低）
- 本项目选 SM-2 而非更新的 FSRS：实现简单、场景适配、可平滑升级

---

## Card 19 | 余弦相似度

**一句话：** 衡量两个向量方向的相似程度，忽略向量长度，值域 [-1, 1]。

**核心概念：**
```
公式：
cos(A, B) = (A · B) / (||A|| × ||B||)

直觉理解：
├── cos = 1   → 方向完全相同（语义一致）
├── cos = 0   → 正交（无关）
└── cos = -1  → 方向相反（语义相反）

Python 实现：
import numpy as np
def cosine_similarity(a, b):
    return np.dot(a, b) / (np.linalg.norm(a) * np.linalg.norm(b))

为什么不用欧氏距离？
├── 欧氏距离受向量长度影响（长文本 vs 短文本向量长度不同）
└── 余弦只看方向，对文本长度不敏感
```

**在本项目中的阈值：**
```
薄弱点去重：≥ 0.75（严格，判断"相同"）
题目图谱建边：≥ 0.65（宽松，发现"相关"）
```

**面试记忆点：**
- Embedding 向量通常已经归一化（长度=1），此时余弦相似度 = 点积
- 阈值没有"标准答案"——取决于嵌入模型和具体场景，需要实验调优
- O(n) 暴力搜索在千级向量时够用；万级以上需要 ANN（FAISS/HNSW）

---

# 七、部署与运维层

---

## Card 20 | Docker + Docker Compose

**一句话：** 容器化技术。Docker 打包应用为镜像，Compose 编排多个容器协同运行。

**核心概念：**
```
Dockerfile 关键指令：
FROM    → 基础镜像
COPY    → 复制文件到容器
RUN     → 执行构建命令
CMD     → 容器启动命令
EXPOSE  → 声明端口

多阶段构建（本项目前端）：
Stage 1 (builder): Node.js + npm ci + npm run build → 产出 dist/
Stage 2 (final):   Nginx + COPY dist/ → 最终镜像（几十 MB，不含 Node.js）

Docker Compose：
services:
  backend:  { build: ./backend,  ports: ["9001:8000"], volumes: [./data:/app/data] }
  frontend: { build: ./frontend, ports: ["9000:80"],   depends_on: [backend] }
```

**在本项目中：**
- `docker-compose.yml` 编排前后端两个服务
- `./data` 目录挂载为 volume，数据持久化
- `.env` 文件挂载注入配置（不 bake 进镜像）

**面试记忆点：**
- 多阶段构建大幅缩小镜像体积（几百 MB → 几十 MB）
- `depends_on` 只控制启动顺序，不保证服务就绪——需要应用层重试
- volume 挂载 vs named volume：挂载更透明（宿主机可直接查看），named volume 更安全

---

## Card 21 | Nginx 反向代理

**一句话：** 高性能 Web 服务器 / 反向代理，生产环境中位于客户端和应用服务器之间。

**核心概念：**
```
本项目的 Nginx 角色：

客户端 → Nginx ┬→ /            → 静态文件（React 构建产物）
               ├→ /api/*       → 代理到 FastAPI (backend:8000)
               └→ 其他路由      → try_files → index.html（SPA 回退）

关键配置：
location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;    # SPA 路由回退
}

location /api/ {
    proxy_pass http://backend:8000;
    proxy_buffering off;                  # SSE 必须关闭缓冲！
}
```

**在本项目中：**
- 提供前端静态文件服务（比 Node.js 快几倍）
- 反向代理 API 请求到 FastAPI 后端
- `try_files` 实现 SPA 客户端路由支持
- `proxy_buffering off` 确保 SSE 流式输出不被缓冲

**面试记忆点：**
- `try_files $uri $uri/ /index.html` 含义：尝试匹配文件 → 尝试匹配目录 → 回退到 index.html
- 没有 `proxy_buffering off`，SSE 数据会被 Nginx 缓冲到一起发送（用户看到一次性输出而非逐字）
- Docker 内部网络中，`backend:8000` 中的 `backend` 是 Compose 服务名（自动 DNS 解析）

---

# 八、设计模式层

---

## Card 22 | 双写持久化模式

**一句话：** 写入时同时更新内存缓存和数据库，读取时内存优先、数据库兜底。

**核心概念：**
```
写入流程：
data → Memory Dict (O(1) 读取)
     → SQLite DB   (持久化兜底)

读取流程：
Memory Dict 命中？ → 返回
        ↓ 未命中
SQLite 查询 → 反序列化 → 回填 Memory → 返回

优势：
├── 读取：内存级速度（纳秒）
├── 持久化：重启后从 DB 恢复
└── 一致性：写入原子化（先内存后 DB）

劣势：
├── 内存消耗：数据存两份
└── 一致性风险：内存写成功但 DB 写失败（概率极低）
```

**在本项目中：**
- 面试会话、刷题会话、算法会话都采用双写
- LangChain Message 对象需要自定义序列化/反序列化
- 24 小时自动清理过期会话

---

## Card 23 | 写时失效缓存策略

**一句话：** 数据更新时只删除缓存（不重建），下次读取时按需重建。

**核心概念：**
```
三种缓存更新策略：
├── Write-Through：写时同时更新缓存和数据源
├── Write-Behind：写入缓存后异步更新数据源
└── Invalidate on Write（本项目）：写时删除缓存，下次读触发重建

适用场景：
├── 重建成本高（如索引构建需要几秒）
├── 写入频率低（知识库更新不频繁）
└── 读取频率高（每次面试都要检索索引）

本项目的索引缓存：
UPDATE 知识库文件
  → 删除内存缓存 _index_cache.pop()
  → 删除磁盘缓存 shutil.rmtree()
  → 下次读取时自动重建索引（L3）
```

**面试记忆点：**
- "写时失效"比"写时重建"更好——用户更新知识库不需要等待索引构建
- 缓存击穿风险：如果大量请求同时触发重建 → 可以用锁或单例构建器保护
- 本项目三级缓存（内存 → 磁盘 → 重建）兼顾了速度和可靠性

---

## Card 24 | 单例模式（Singleton）

**一句话：** 确保一个类/资源全局只有一个实例，避免重复创建的开销。

**核心概念：**
```python
# Python 模块级单例（本项目的实现方式）
_embedding_instance = None

def get_embedding_model():
    global _embedding_instance
    if _embedding_instance is None:
        _embedding_instance = _init_embedding()  # 首次调用时初始化
    return _embedding_instance

# 为什么要单例？
# 本地 Embedding 模型加载需要 5-10 秒 + 几百 MB 内存
# 如果每次请求都加载 → 响应延迟爆炸 + 内存溢出
```

**在本项目中：**
- `_embedding_instance`：嵌入模型单例
- `_llama_llm_instance`：LlamaIndex LLM 单例
- `_index_cache`：虽然是 dict 不是单例，但思路类似——全局共享避免重复构建

**面试记忆点：**
- Python 模块本身就是单例（模块只加载一次），模块级变量天然是全局唯一
- 线程安全问题：FastAPI 多线程环境下需要注意竞态条件（可用 `threading.Lock` 保护）
- 单例 vs 依赖注入：FastAPI 的 `Depends()` 也能实现类似效果，但不缓存实例

---

# 九、快速索引

| Card | 技术 | 一句话 | 项目角色 |
|------|------|--------|---------|
| 01 | FastAPI | 异步 Web 框架 | API 服务承载 |
| 02 | Pydantic | 数据校验 | 请求参数校验 + 配置管理 |
| 03 | Uvicorn | ASGI 服务器 | FastAPI 运行容器 |
| 04 | LangChain | LLM 编排框架 | 消息抽象 + 工具调用 |
| 05 | LangGraph | 图执行引擎 | 面试状态机 |
| 06 | LlamaIndex | RAG 框架 | 知识库 + 简历检索 |
| 07 | Embedding | 文本向量化 | 语义检索基础 |
| 08 | RAG | 检索增强生成 | 知识增强出题 |
| 09 | SQLite + WAL | 嵌入式数据库 | 全部结构化存储 |
| 10 | JWT | 无状态认证 | 用户认证 |
| 11 | bcrypt | 密码哈希 | 密码安全存储 |
| 12 | React 19 | 声明式 UI | 前端框架 |
| 13 | React Router v7 | 客户端路由 | 页面导航 + 路由守卫 |
| 14 | Vite | 构建工具 | 开发服务器 + 生产打包 |
| 15 | Tailwind CSS v4 | 原子化 CSS | 全部样式 |
| 16 | Radix UI | 无样式组件库 | UI 组件行为 + 无障碍 |
| 17 | SSE | 服务端推送 | AI 助手流式输出 |
| 18 | SM-2 | 间隔重复算法 | 薄弱点复习调度 |
| 19 | 余弦相似度 | 向量相似度量 | 去重 + 图谱建边 |
| 20 | Docker Compose | 容器编排 | 一键部署 |
| 21 | Nginx | 反向代理 | 静态服务 + API 代理 |
| 22 | 双写持久化 | 缓存 + DB | 会话容灾 |
| 23 | 写时失效 | 缓存策略 | 索引缓存一致性 |
| 24 | 单例模式 | 全局唯一实例 | 模型加载优化 |
| 25 | async/await | 协程异步编程 | FastAPI 异步路由 |
| 26 | 装饰器 | 函数包装语法糖 | 路由注册/校验器 |
| 27 | 类型提示 | 静态类型注解 | TypedDict/Pydantic |
| 28 | 上下文管理器 | with 资源管理 | DB 连接/临时文件 |
| 29 | RESTful API | 资源导向设计 | 路由设计规范 |
| 30 | CORS | 跨域资源共享 | 前后端分离开发 |
| 31 | HTTP 状态码 | 响应语义表达 | 错误处理规范 |
| 32 | 环境变量 | 配置与代码分离 | .env + BaseSettings |
| 33 | pytest | 单元测试框架 | fixture + 参数化 |
| 34 | httpx TestClient | API 测试 | 异步接口测试 |
| 35 | Git | 版本控制 | 分支管理/协作 |
| 36 | CI/CD | 持续集成部署 | 自动测试+自动发布 |
| 37 | 数据库索引 | B+Tree 快速查找 | 查询性能优化 |
| 38 | 缓存策略 | 穿透/击穿/雪崩 | 缓存一致性保障 |
| 39 | 消息队列 | 异步解耦削峰 | SSE/asyncio.Queue |
| 40 | 系统设计总结 | 全景架构串联 | 面试叙事模板 |
| 41 | React Hooks | 状态/副作用/缓存 | 函数组件核心 API |
| 42 | Prompt Engineering | 提示工程设计 | LLM 输出质量控制 |
| 43 | WebSocket vs SSE | 推送技术对比 | 选型决策依据 |
| 44 | 限流与降级 | 令牌桶/熔断器 | 系统高可用防护 |
| 45 | 可观测性 | 日志/指标/追踪 | 线上排障三支柱 |

---

# 十、Python 语言基础层

---

## Card 25 | Python async/await 与 asyncio

**一句话：** Python 的协程异步编程模型，单线程内通过事件循环实现并发 I/O。

**核心概念：**
```
同步 vs 异步：
同步：result = requests.get(url)           # 阻塞等待 2 秒
异步：result = await aiohttp.get(url)      # 挂起，事件循环执行其他任务

关键组件：
├── async def   → 定义协程函数（返回 coroutine 对象）
├── await       → 暂停当前协程，等待异步操作完成
├── Event Loop  → 调度所有协程的执行（asyncio.run() 启动）
├── Task        → 协程的包装，可并发执行
└── gather()    → 并发执行多个协程

并发 vs 并行：
├── asyncio 是并发（Concurrency）：单线程，I/O 等待时切换任务
├── threading 是并行（Parallelism）：多线程，真正同时执行
└── asyncio 适合 I/O 密集（网络请求、DB 查询）
    threading 适合 CPU 密集（计算、图片处理）
```

**在本项目中：**
```python
# FastAPI 异步路由
async def get_profile(user_id: str = Depends(get_current_user)):
    profile = await load_profile(user_id)    # 异步读文件
    sessions = await get_sessions(user_id)   # 异步查 DB
    return {"profile": profile, "sessions": sessions}

# aiosqlite 异步数据库
async with aiosqlite.connect(DB_PATH) as db:
    cursor = await db.execute("SELECT ...", params)
    rows = await cursor.fetchall()
```

**面试记忆点：**
- `async def` 函数必须用 `await` 调用，否则只得到一个 coroutine 对象不会执行
- FastAPI 中 `async def` 路由在事件循环中执行；普通 `def` 路由自动放入线程池
- Python 的 GIL 使得多线程无法真正并行 CPU 任务，但 asyncio 不受 GIL 影响（因为是单线程）
- `await asyncio.to_thread(sync_func)` 可以在异步上下文中调用同步阻塞函数

---

## Card 26 | Python 装饰器（Decorator）

**一句话：** 不修改原函数代码，在其外层包装额外逻辑的语法糖。

**核心概念：**
```python
# 装饰器本质：接收函数，返回新函数
def my_decorator(func):
    def wrapper(*args, **kwargs):
        print("before")
        result = func(*args, **kwargs)
        print("after")
        return result
    return wrapper

@my_decorator        # 语法糖，等价于 hello = my_decorator(hello)
def hello():
    print("hello")

# 带参数的装饰器（三层嵌套）
def repeat(n):
    def decorator(func):
        def wrapper(*args, **kwargs):
            for _ in range(n):
                func(*args, **kwargs)
        return wrapper
    return decorator

@repeat(3)
def say_hi():
    print("hi")
```

**在本项目中：**
```python
# FastAPI 路由装饰器
@app.post("/api/interview/start")     # 注册路由
@app.on_event("startup")              # 生命周期钩子

# Pydantic 装饰器
@field_validator("email")             # 字段校验器
```

**面试记忆点：**
- `@decorator` 是语法糖，等价于 `func = decorator(func)`
- FastAPI 的 `@app.get()` / `@app.post()` 实质上将函数注册到路由表
- 用 `functools.wraps(func)` 保留原函数的 `__name__` 和 `__doc__`
- 类也可以做装饰器（实现 `__call__` 方法）

---

## Card 27 | Python 类型提示（Type Hints）

**一句话：** Python 3.5+ 的类型注解系统，不影响运行时但提升代码可读性和工具支持。

**核心概念：**
```python
# 基础类型
def greet(name: str) -> str:
    return f"Hello {name}"

# 复合类型
from typing import Optional, Union
scores: list[int] = [8, 7, 9]
config: dict[str, Any] = {"model": "gpt-4"}
maybe_topic: Optional[str] = None        # 等价于 str | None
result: Union[str, int] = "ok"           # 等价于 str | int (3.10+)

# TypedDict（LangGraph State 的基础）
from typing import TypedDict, Annotated
class InterviewState(TypedDict):
    messages: Annotated[list, add_messages]  # 带注解的类型
    phase: str
    resume_context: str

# Python 3.10+ 新语法
def process(data: str | None = None) -> list[str]:  # 替代 Optional/Union
    ...
```

**在本项目中：**
- `models.py`：TypedDict 定义 LangGraph 状态结构
- `config.py`：Pydantic BaseSettings 的字段类型注解
- 所有 API 路由函数的参数和返回值类型注解
- `Annotated[list, add_messages]` 是 LangGraph 的消息追加标记

**面试记忆点：**
- 类型提示是**静态检查用的**，运行时不强制（Pydantic 除外——它做运行时校验）
- `TypedDict` vs `dataclass` vs `BaseModel`：TypedDict 是 dict 子类（轻量），dataclass 是普通类，BaseModel 有校验
- `Annotated` 可以附加元数据——LangGraph 用它定义 state 的合并策略

---

## Card 28 | Python 上下文管理器（Context Manager）

**一句话：** `with` 语句的底层机制，自动管理资源的获取和释放（文件、连接、锁）。

**核心概念：**
```python
# 标准用法
with open("file.txt") as f:    # __enter__() 打开文件
    data = f.read()             # 使用文件
# 离开 with 块 → __exit__() 自动关闭文件（即使异常也会关闭）

# 异步上下文管理器（本项目核心模式）
async with aiosqlite.connect(DB_PATH) as db:   # __aenter__()
    await db.execute("INSERT ...")
# __aexit__() 自动关闭连接 + 提交/回滚事务

# 自定义上下文管理器
from contextlib import contextmanager

@contextmanager
def timer():
    start = time.time()
    yield                       # yield 前 = __enter__，yield 后 = __exit__
    print(f"耗时: {time.time() - start}s")

with timer():
    do_something()
```

**在本项目中：**
```python
# 每次 DB 操作都用 async with 确保连接释放
async with aiosqlite.connect(DB_PATH) as db:
    db.row_factory = aiosqlite.Row
    ...

# 临时文件（录音转写）
with tempfile.NamedTemporaryFile(suffix=".webm") as tmp:
    tmp.write(audio_bytes)
    # 离开 with 后临时文件自动删除
```

**面试记忆点：**
- `with` 保证资源释放——即使代码抛异常，`__exit__` 也会执行
- `async with` 是异步版本，用于异步资源（aiosqlite、aiohttp）
- 不用 `with` 的常见 bug：数据库连接泄漏、文件句柄耗尽

---

# 十一、API 设计与网络协议层

---

## Card 29 | RESTful API 设计原则

**一句话：** 以资源为中心、用 HTTP 方法表达操作、无状态的 API 设计风格。

**核心概念：**
```
REST 六大约束：
├── Client-Server      → 前后端分离
├── Stateless          → 每次请求携带全部信息（JWT），服务端不存会话
├── Cacheable          → 响应可缓存（GET 幂等）
├── Uniform Interface  → 统一的资源 URI + HTTP 方法语义
├── Layered System     → 可加 Nginx/CDN 等中间层
└── Code on Demand     → 可选，服务端返回可执行代码

HTTP 方法 → CRUD 映射：
GET    /users          → 查询列表（幂等、可缓存）
GET    /users/{id}     → 查询单个
POST   /users          → 创建（非幂等）
PUT    /users/{id}     → 全量替换（幂等）
PATCH  /users/{id}     → 部分更新（幂等）
DELETE /users/{id}     → 删除（幂等）

URI 设计规范：
✅ /api/interviews/{id}/questions    → 名词复数、层级关系
✅ /api/sessions?status=active       → 过滤用查询参数
❌ /api/getInterviewById             → 不用动词（GET 已表达"获取"）
❌ /api/interview_list               → 不用下划线，不用 list
```

**在本项目中：**
```python
# main.py 路由设计
POST   /api/auth/register          # 注册
POST   /api/auth/login             # 登录
GET    /api/interview/sessions     # 查询面试列表
POST   /api/interview/start        # 创建新面试
POST   /api/interview/{id}/answer  # 提交回答
GET    /api/knowledge/graph         # 获取知识图谱
POST   /api/ai-assistant/query     # AI 助手提问
```

**面试记忆点：**
- **幂等性**：GET/PUT/DELETE 多次调用结果一样；POST 不幂等（每次创建新资源）
- REST 不是协议/标准，是**架构风格**——没有强制规范，只有最佳实践
- 本项目部分接口用 POST 做查询（如 AI 助手），因为需要请求体传复杂参数——这是合理的实用权衡
- 面试常问：「PUT 和 PATCH 的区别？」—— PUT 全量替换，PATCH 部分更新

---

## Card 30 | CORS（跨域资源共享）

**一句话：** 浏览器安全机制，控制网页能否请求不同域名的 API。

**核心概念：**
```
同源策略（Same-Origin Policy）：
浏览器默认阻止 JS 跨域请求
  同源 = 协议 + 域名 + 端口 完全一致
  http://localhost:5173  →  http://localhost:8000   ❌ 端口不同 = 跨域

CORS 流程：
1. 简单请求（GET/POST + 简单 Header）
   浏览器直接发请求，带 Origin 头
   → 服务端返回 Access-Control-Allow-Origin
   → 浏览器检查：匹配则放行，否则拦截

2. 预检请求（PUT/DELETE 或自定义 Header）
   浏览器先发 OPTIONS 请求（Preflight）
   → 服务端返回允许的方法/头部
   → 通过后浏览器才发真正请求

关键响应头：
Access-Control-Allow-Origin:  http://localhost:5173
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization
Access-Control-Allow-Credentials: true   # 允许带 Cookie/JWT
```

**在本项目中：**
```python
# main.py — FastAPI CORS 中间件
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins,  # ["http://localhost:5173"]
    allow_credentials=True,               # 允许携带认证信息
    allow_methods=["*"],                  # 所有 HTTP 方法
    allow_headers=["*"],                  # 所有 Header
)
# 开发环境：前端 5173 → 后端 8000，必须配置 CORS
# 生产环境：Nginx 反代后同源，CORS 可选
```

**面试记忆点：**
- CORS 是**浏览器行为**，不是服务端限制——Postman/curl 不受 CORS 影响
- `allow_origins=["*"]` + `allow_credentials=True` 不能同时使用（浏览器拒绝）
- 预检请求是 OPTIONS 方法——如果后端不处理 OPTIONS，PUT/DELETE 请求会失败
- 生产环境通过 Nginx 反代让前后端同源，从根本上绕过 CORS

---

## Card 31 | HTTP 状态码与错误处理

**一句话：** HTTP 响应状态码表达请求结果，合理使用状态码是 API 设计的基本素养。

**核心概念：**
```
状态码分类：
1xx 信息      → 100 Continue（SSE 连接建立前）
2xx 成功      → 200 OK / 201 Created / 204 No Content
3xx 重定向    → 301 永久 / 302 临时 / 304 Not Modified（缓存命中）
4xx 客户端错误 → 400 Bad Request / 401 Unauthorized / 403 Forbidden / 404 Not Found / 422 Validation Error
5xx 服务端错误 → 500 Internal Error / 502 Bad Gateway / 503 Service Unavailable

面试高频辨析：
401 vs 403：
├── 401 Unauthorized → 未认证（没带 Token / Token 过期）
└── 403 Forbidden    → 已认证但无权限（普通用户访问管理接口）

400 vs 422：
├── 400 Bad Request         → 请求格式错误（JSON 解析失败）
└── 422 Unprocessable Entity → 格式正确但语义错误（email 不合法）
    FastAPI/Pydantic 校验失败默认返回 422
```

**在本项目中：**
```python
# 认证失败 → 401
raise HTTPException(status_code=401, detail="Invalid credentials")

# 资源不存在 → 404
raise HTTPException(status_code=404, detail="Session not found")

# Pydantic 校验失败 → 422（FastAPI 自动处理）
class RegisterRequest(BaseModel):
    email: EmailStr    # 非法 email → 自动 422

# SSE 流 → 200 + text/event-stream
return StreamingResponse(event_generator(), media_type="text/event-stream")
```

**面试记忆点：**
- FastAPI + Pydantic 的校验错误返回 **422** 而不是 400——这是 FastAPI 的设计选择
- SSE 连接成功是 200（不是 101——101 是 WebSocket 升级）
- 502 Bad Gateway 通常是 Nginx 转发到 Uvicorn 时后端没启动
- 设计 API 时，错误响应应包含机器可读的 `code` + 人可读的 `message`

---

## Card 32 | 环境变量与密钥管理

**一句话：** 将配置与代码分离，敏感信息不硬编码、不入版本控制。

**核心概念：**
```
十二要素应用（12-Factor App）第三条：
"在环境中存储配置"

配置层级（优先级从高到低）：
1. 环境变量       → export API_KEY=xxx（最高优先级）
2. .env 文件      → 开发用，python-dotenv 加载
3. 配置文件默认值  → config.py 中的 default 值
4. 硬编码         → ❌ 永远不应该硬编码密钥

.env 文件规范：
├── .env          → 本地开发配置（加入 .gitignore）
├── .env.example  → 模板文件（提交到 Git，不含真实密钥）
└── .env.prod     → 生产配置（服务器上，不入 Git）

敏感信息清单：
├── API Key（OpenAI、LLM 服务）
├── JWT Secret Key
├── 数据库连接字符串（含密码时）
├── SMTP 密码
└── 第三方 OAuth Client Secret
```

**在本项目中：**
```python
# config.py — Pydantic BaseSettings 自动读取环境变量
class Settings(BaseSettings):
    openai_api_key: str = ""           # 从 .env 或环境变量读取
    jwt_secret: str = "change-me"      # 开发默认值，生产必须覆盖
    database_url: str = "sqlite:///data/techspar.db"

    model_config = SettingsConfigDict(
        env_file=".env",               # 自动加载 .env 文件
        env_file_encoding="utf-8",
    )

# .gitignore
*.env
.env*
!.env.example
```

**面试记忆点：**
- **永远不要**把 API Key 提交到 Git——即使后来删了，历史记录里还在
- Pydantic BaseSettings 的优先级：环境变量 > .env 文件 > 代码默认值
- Docker 中通过 `docker-compose.yml` 的 `env_file` 或 `environment` 传入
- 生产环境最佳实践：用 Vault/AWS Secrets Manager 等密钥管理服务，不用 .env 文件

---

# 十二、测试与质量保障层

---

## Card 33 | pytest 单元测试

**一句话：** Python 最主流的测试框架，用简洁的 assert 语句 + fixture 机制覆盖代码逻辑。

**核心概念：**
```
pytest 核心三件套：
├── test 函数    → 函数名以 test_ 开头即可被发现
├── assert       → 直接用 Python 原生 assert（不需要 self.assertEqual）
└── fixture      → 测试前的准备工作（创建数据、Mock 服务等）

# 基础测试
def test_add():
    assert add(1, 2) == 3
    assert add(-1, 1) == 0

# fixture 依赖注入
@pytest.fixture
def db_session():
    session = create_test_db()
    yield session              # yield 前 = setup, yield 后 = teardown
    session.close()

def test_create_user(db_session):   # 自动注入 fixture
    user = create_user(db_session, "test@example.com")
    assert user.email == "test@example.com"

# 参数化测试（一个测试函数跑多组数据）
@pytest.mark.parametrize("input,expected", [
    ("hello", 5),
    ("", 0),
    ("中文", 2),
])
def test_string_length(input, expected):
    assert len(input) == expected

测试分层：
├── 单元测试   → 测函数/类，Mock 外部依赖
├── 集成测试   → 测组件组合，真实 DB/真实服务
└── E2E 测试   → 测完整流程，真实请求
```

**在本项目中：**
```python
# 测试 FastAPI 接口
from httpx import AsyncClient

@pytest.fixture
async def client():
    async with AsyncClient(app=app, base_url="http://test") as ac:
        yield ac

async def test_register(client):
    resp = await client.post("/api/auth/register", json={
        "email": "test@test.com",
        "password": "Test123!"
    })
    assert resp.status_code == 200
    assert "token" in resp.json()
```

**面试记忆点：**
- pytest 的 fixture 是**依赖注入**——参数名匹配 fixture 名即自动注入
- `yield` fixture 兼顾 setup + teardown，比 unittest 的 setUp/tearDown 更灵活
- `conftest.py` 中的 fixture 自动对同目录及子目录的所有测试可用
- 运行命令：`pytest -v`（详细）、`pytest -k "test_login"`（按名过滤）、`pytest --cov`（覆盖率）

---

## Card 34 | httpx + FastAPI TestClient

**一句话：** 在测试中模拟 HTTP 请求访问 FastAPI 应用，无需真正启动服务器。

**核心概念：**
```
测试 FastAPI 的两种方式：

1. TestClient（同步，基于 requests）
   from fastapi.testclient import TestClient
   client = TestClient(app)
   resp = client.get("/api/health")

2. AsyncClient（异步，基于 httpx）—— 推荐
   from httpx import AsyncClient
   async with AsyncClient(app=app, base_url="http://test") as ac:
       resp = await ac.get("/api/health")

为什么推荐 AsyncClient？
├── FastAPI 是异步框架，用异步测试更自然
├── 能测试 async def 路由的完整行为
├── 能测试 SSE 流式响应
└── 能测试 WebSocket 连接

Mock 外部依赖：
# 用 dependency_overrides 替换真实依赖
def override_get_llm():
    return MockLLM()

app.dependency_overrides[get_llm] = override_get_llm
```

**在本项目中：**
```python
# 测试面试流程
async def test_interview_flow(client, auth_headers):
    # 1. 开始面试
    start = await client.post("/api/interview/start",
        json={"job_title": "Python Engineer"},
        headers=auth_headers)
    session_id = start.json()["session_id"]

    # 2. 提交回答
    answer = await client.post(f"/api/interview/{session_id}/answer",
        json={"content": "I would use FastAPI..."},
        headers=auth_headers)
    assert answer.status_code == 200
```

**面试记忆点：**
- `TestClient` 是同步的（底层 requests），`AsyncClient` 是异步的（底层 httpx）
- `app.dependency_overrides` 是 FastAPI 的 Mock 机制——替换 Depends() 注入的依赖
- 测试不需要真正启动 Uvicorn——ASGI 协议允许直接调用 app

---

# 十三、工程化与协作层

---

## Card 35 | Git 版本控制核心概念

**一句话：** 分布式版本控制系统，跟踪代码每一次变更，支持多人协作与分支管理。

**核心概念：**
```
Git 三区模型：
Working Directory → Staging Area → Repository
   工作区              暂存区         本地仓库
  (修改文件)        (git add)       (git commit)

核心操作：
git add .              → 工作区 → 暂存区
git commit -m "msg"    → 暂存区 → 本地仓库
git push origin main   → 本地仓库 → 远程仓库
git pull               → fetch + merge（获取远程 + 合并）

分支策略（Git Flow 简化版）：
main       ────●────────●────────●──── 稳定版本
                \      /          \
feature/xxx      ●──●──           ● 功能分支
                                 /
hotfix/xxx                   ●──● 紧急修复

常用命令：
git branch feature/xxx       → 创建分支
git checkout feature/xxx     → 切换分支（或 git switch）
git merge feature/xxx        → 合并分支
git rebase main              → 变基（线性历史）
git stash                    → 暂存未提交的修改
git log --oneline --graph    → 查看提交历史
```

**在本项目中：**
```
TechSpar 分支管理：
├── main           → 生产版本
├── develop        → 开发主线
├── feature/*      → 功能开发
└── .gitignore     → 排除 .env / node_modules / __pycache__ / data/

提交信息规范（Conventional Commits）：
feat: 新功能         → feat: add AI assistant SSE streaming
fix: 修复 bug        → fix: JWT token expiration check
docs: 文档更新       → docs: update API documentation
refactor: 重构       → refactor: extract interview state machine
```

**面试记忆点：**
- `merge` vs `rebase`：merge 保留分支历史（有合并节点），rebase 线性化历史（更整洁）
- `git reset --soft` 保留修改在暂存区，`--mixed` 保留在工作区，`--hard` 全部丢弃
- `.gitignore` 只对**未追踪**的文件生效——已提交的文件需要 `git rm --cached` 先移除跟踪
- 面试常问：「如何撤销最近一次 commit？」→ `git reset --soft HEAD~1`

---

## Card 36 | CI/CD 持续集成与持续部署

**一句话：** 代码推送后自动运行测试、构建、部署的工程化流水线。

**核心概念：**
```
CI（Continuous Integration）→ 持续集成：
  代码提交 → 自动运行测试 → 代码质量检查 → 反馈结果
  目的：尽早发现 Bug，保证主分支稳定

CD（Continuous Deployment）→ 持续部署：
  测试通过 → 自动构建镜像 → 自动部署到服务器
  目的：缩短从代码到上线的时间

典型 CI/CD 流水线：
push → lint → test → build → deploy
  │       │       │       │       │
  │    代码规范  单元测试  Docker  自动发布
  │    (ruff)  (pytest)  镜像构建  到服务器

常见工具：
├── GitHub Actions → GitHub 原生 CI/CD（本项目使用）
├── GitLab CI      → GitLab 原生
├── Jenkins        → 老牌自托管
└── Docker Hub     → 镜像自动构建
```

**在本项目中：**
```yaml
# .github/workflows/ci.yml（概念示例）
name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - run: pip install -r requirements.txt
      - run: pytest --cov=app tests/

  build:
    needs: test    # test 通过后才构建
    steps:
      - run: docker build -t techspar .
      - run: docker push registry/techspar:latest

  deploy:
    needs: build   # 构建通过后才部署
    steps:
      - run: ssh server "docker compose pull && docker compose up -d"
```

**面试记忆点：**
- CI 的核心价值：每次提交都自动验证，**防止 "在我电脑上能跑" 的问题**
- GitHub Actions 的 `needs` 字段定义 job 间依赖——实现流水线串联
- Docker 使部署环境一致——镜像里包含了所有依赖，消除环境差异
- 面试常问：「你的项目怎么部署的？」→ 推代码 → CI 跑测试 → Docker 构建 → 服务器拉取最新镜像 → docker compose up

---

# 十四、系统设计面试专题

---

## Card 37 | 数据库索引原理

**一句话：** 为数据库表的某些列建立快速查找结构（B+Tree），将全表扫描变为对数级查找。

**核心概念：**
```
无索引 vs 有索引：
SELECT * FROM sessions WHERE user_id = 'abc'
  无索引 → 全表扫描 O(n)，逐行检查
  有索引 → B+Tree 查找 O(log n)，直接定位

索引类型：
├── 主键索引（PRIMARY KEY）→ 自动创建，唯一 + 非空
├── 唯一索引（UNIQUE）     → 保证列值唯一
├── 普通索引（INDEX）      → 加速查询
├── 复合索引              → 多列联合索引，遵循最左匹配原则
└── 全文索引              → 文本搜索（本项目用向量检索替代）

B+Tree 结构（面试核心）：
         [30 | 60]                    ← 根节点（非叶子）
        /    |    \
   [10|20] [40|50] [70|80]           ← 中间节点
    / | \   / | \   / | \
  叶子节点（存储实际数据指针，叶子间有链表连接）

为什么用 B+Tree 而不是 Hash：
├── B+Tree 支持范围查询（> < BETWEEN）和排序（ORDER BY）
├── Hash 只支持精确匹配（=）
└── B+Tree 的叶子节点链表使得范围扫描高效

索引的代价：
├── 空间：每个索引占额外存储
├── 写入：INSERT/UPDATE/DELETE 需要同步更新索引
└── 选择性：低选择性的列（如性别）建索引收益小
```

**在本项目中：**
```sql
-- SQLite 索引（init_db 中创建）
CREATE INDEX idx_sessions_user ON interview_sessions(user_id);
CREATE INDEX idx_questions_session ON interview_questions(session_id);
CREATE INDEX idx_weak_user ON weak_points(user_id);

-- 查询优化前后对比：
-- 无索引：查某用户的 50 次面试 → 扫描全部 10 万条 session
-- 有索引：B+Tree 直接定位 → 只读取 50 条
```

**面试记忆点：**
- **最左匹配原则**：复合索引 (a, b, c) 只能用于 a / a+b / a+b+c 的查询，不能跳过 a 直接用 b
- SQLite 默认 `rowid` 就是主键索引（B-Tree）
- `EXPLAIN QUERY PLAN` 可以查看查询是否使用了索引
- 面试常问：「什么情况下索引失效？」→ `LIKE '%xxx'`（前缀通配）/ 函数包裹列 / 类型不匹配 / OR 条件

---

## Card 38 | 缓存策略与常见问题

**一句话：** 在数据库和应用之间加一层快速存储，减少重复计算和数据库压力。

**核心概念：**
```
缓存三大问题（面试必问）：

1. 缓存穿透（Cache Penetration）
   查询不存在的数据 → 缓存 miss → 打到 DB → DB 也没有
   ❌ 攻击者用大量不存在的 key 直接打穿 DB
   ✅ 解法：缓存空值（短 TTL）/ 布隆过滤器

2. 缓存击穿（Cache Breakdown）
   热点 key 过期瞬间 → 大量请求同时打到 DB
   ❌ DB 瞬间压力暴增
   ✅ 解法：互斥锁（只允许一个请求回源）/ 热点 key 永不过期

3. 缓存雪崩（Cache Avalanche）
   大量 key 同时过期 → 请求全部打到 DB
   ❌ DB 被压垮
   ✅ 解法：过期时间加随机偏移 / 多级缓存 / 限流降级

缓存更新策略：
├── Cache Aside     → 读先查缓存，miss 则查 DB 并写回缓存（最常用）
├── Write Through   → 写操作同时更新缓存和 DB
├── Write Behind     → 写只更新缓存，异步刷回 DB（高性能但有数据丢失风险）
└── Write Invalidate → 写操作删除缓存，下次读时重建（本项目采用）
```

**在本项目中：**
```python
# 本项目的缓存策略
# 1. 写时失效（Card 23）
async def update_session(session_id, data):
    await db.execute("UPDATE ...", data)     # 写 DB
    cache.delete(f"session:{session_id}")    # 删缓存

# 2. 双写持久化（Card 22）
async def save_message(session_id, msg):
    cache.set(f"session:{session_id}", msg)  # 写缓存
    await db.execute("INSERT ...", msg)       # 写 DB

# 缓存穿透防护：查询结果为空时缓存短 TTL 空值
if result is None:
    cache.set(key, EMPTY_SENTINEL, ttl=60)
```

**面试记忆点：**
- 穿透 = 查不存在的；击穿 = 热点 key 过期；雪崩 = 大量 key 同时过期
- 本项目用内存字典做缓存（单实例够用），生产大规模用 Redis
- Cache Aside 是最安全的策略——先更新 DB 再删缓存，最多短暂不一致
- 面试画图：请求 → 缓存命中？→ 是 → 返回 / 否 → 查 DB → 写回缓存 → 返回

---

## Card 39 | 消息队列基础概念

**一句话：** 在生产者和消费者之间加一层异步缓冲，实现解耦、削峰、异步处理。

**核心概念：**
```
消息队列模型：
Producer → [Message Queue] → Consumer
生产者        消息缓冲区       消费者

为什么要用消息队列：
├── 解耦：服务间不直接调用，通过消息通信
├── 削峰：高并发时消息排队，防止下游被压垮
├── 异步：耗时操作放入队列后台处理，快速响应用户
└── 可靠：消息持久化，消费者宕机后可重试

两种模式：
├── 点对点（P2P）→ 一条消息只被一个消费者消费
└── 发布/订阅（Pub/Sub）→ 一条消息被所有订阅者消费

常见实现：
├── Redis Streams   → 轻量级，适合小规模
├── RabbitMQ        → 功能完善，AMQP 协议
├── Kafka           → 高吞吐，日志/事件流
└── 内存队列        → asyncio.Queue（单进程内）

关键概念：
├── ACK（确认）     → 消费者处理完后确认，否则消息重新入队
├── Dead Letter     → 多次消费失败的消息进入死信队列
├── 幂等性          → 消费者需处理重复消息（消息可能重复投递）
└── 有序性          → 同一分区/队列内消息有序
```

**在本项目中（概念映射）：**
```python
# 本项目未使用独立消息队列，但有类似模式：

# 1. SSE 流 ≈ 简单的发布/订阅
async def event_generator():
    async for chunk in llm.astream(messages):
        yield f"data: {json.dumps(chunk)}\n\n"   # 生产消息
# 浏览器 EventSource 订阅                         # 消费消息

# 2. asyncio.Queue 可用于进程内异步任务
import asyncio
queue = asyncio.Queue()
await queue.put(task)          # 生产
task = await queue.get()       # 消费

# 3. 如果项目需要扩展（如面试录音转写排队处理）：
# 用户上传录音 → 放入 Redis Queue → Worker 异步转写 → 回调通知
```

**面试记忆点：**
- 消息队列解决三个问题：**解耦、削峰、异步**——这是标准答案
- Kafka vs RabbitMQ：Kafka 高吞吐（日志流）、RabbitMQ 低延迟（业务消息）
- **幂等性**是消费端必须考虑的——网络抖动可能导致消息重复投递
- 面试常问：「如何保证消息不丢？」→ 生产端确认 + 持久化 + 消费端 ACK

---

## Card 40 | 系统设计面试：TechSpar 全景总结

**一句话：** 将前面 39 张卡片的知识串联成一个完整的系统设计叙事。

**系统架构全景：**
```
                           用户浏览器
                              │
                         ┌────▼────┐
                         │  Nginx  │  ← Card 21（反向代理 + 静态服务）
                         └────┬────┘
                    ┌─────────┼─────────┐
                    ▼                   ▼
            React SPA               FastAPI
         ┌──────────────┐      ┌──────────────┐
         │ React 19     │      │ 路由层        │ ← Card 01
         │ React Router │      │ 认证中间件    │ ← Card 10/11
         │ Tailwind     │      │ CORS 中间件   │ ← Card 30
         │ Radix UI     │      │ 依赖注入      │ ← Card 01
         └──────┬───────┘      └──────┬───────┘
                │ SSE(Card 17)        │
                │ fetch               │
                ▼                     ▼
         EventSource           ┌──────────────┐
                               │ 业务层        │
                               │ ├─ 面试引擎   │ ← Card 05 (LangGraph)
                               │ ├─ AI 助手    │ ← Card 04 (LangChain)
                               │ ├─ 知识图谱   │ ← Card 19
                               │ └─ 复习调度   │ ← Card 18 (SM-2)
                               └──────┬───────┘
                                      │
                          ┌───────────┼───────────┐
                          ▼           ▼           ▼
                   ┌──────────┐ ┌──────────┐ ┌──────────┐
                   │ SQLite   │ │ LlamaIndex│ │ LLM API  │
                   │ + WAL    │ │ VectorDB  │ │ (OpenAI) │
                   │ Card 09  │ │ Card 06/07│ │ Card 04  │
                   └──────────┘ └──────────┘ └──────────┘
```

**面试叙事模板（30 秒版）：**
> "TechSpar 是一个 AI 驱动的模拟面试平台。后端用 **FastAPI** 提供 RESTful API，
> 用 **LangGraph** 构建面试状态机实现多轮对话，用 **LlamaIndex** 做 RAG 支持知识增强出题。
> 前端是 **React 19 + Tailwind** 的 SPA，通过 **SSE** 实现 AI 回答的流式输出。
> 数据层用 **SQLite + WAL** 模式做结构化存储，加内存缓存做热点数据加速。
> 部署用 **Docker Compose + Nginx** 反代，CI/CD 自动化测试和发布。"

**面试深挖点准备：**
```
Q: "为什么选 FastAPI 而不是 Flask/Django？"
A: 原生 async 支持 SSE 流式输出；Pydantic 集成做自动校验；
   性能接近 Go/Node.js；自动生成 API 文档加速开发。

Q: "为什么用 SQLite 而不是 PostgreSQL？"
A: 单机部署场景够用；零配置嵌入式；WAL 模式支持读写并发；
   项目规模（万级用户）不需要分布式数据库。
   如果扩展到多实例，会迁移到 PostgreSQL。

Q: "遇到的最大技术挑战？"
A: LangGraph 状态机的设计——面试流程有开场/提问/追问/评估/总结
   多个阶段，需要根据用户回答动态决定下一步。
   用条件边（conditional_edge）实现了灵活的路由逻辑。

Q: "如何保证数据一致性？"
A: 双写持久化（缓存 + DB 同步写入）+ 写时失效策略；
   SQLite WAL 模式保证读写不互斥；
   JWT 无状态认证避免会话同步问题。

Q: "如何优化性能？"
A: 单例模式加载 Embedding 模型（避免重复初始化）；
   LlamaIndex 索引缓存（避免重建向量索引）；
   SSE 流式输出（用户无需等待完整响应）；
   异步 I/O（aiosqlite + async/await 提高并发能力）。
```

**全栈技术词汇速查：**
| 层次 | 技术栈 | 一句话 |
|------|--------|--------|
| 前端框架 | React 19 | 声明式 UI + Hooks |
| 路由 | React Router v7 | SPA 客户端路由 |
| 样式 | Tailwind CSS v4 | 原子化 CSS |
| 组件 | Radix UI | 无障碍行为组件 |
| 构建 | Vite | 极速 HMR + 打包 |
| 后端 | FastAPI | 异步 API 框架 |
| AI 编排 | LangGraph | 状态机图执行引擎 |
| RAG | LlamaIndex | 检索增强生成 |
| 向量化 | Embedding | 文本转向量 |
| 数据库 | SQLite + WAL | 嵌入式 DB |
| 认证 | JWT + bcrypt | 无状态认证 + 密码哈希 |
| 通信 | SSE | 服务端推送流 |
| 算法 | SM-2 / 余弦相似度 | 复习调度 / 语义匹配 |
| 部署 | Docker + Nginx | 容器化 + 反代 |
| 语言 | Python 3.11 | async/await + 类型提示 |

---

# 十五、前端进阶层

---

## Card 41 | React Hooks 深入

**一句话：** Hooks 是 React 16.8+ 的核心 API，让函数组件拥有状态和副作用能力。

**核心概念：**
```
7 个常用 Hooks 速查：

useState     → 组件内状态
useEffect    → 副作用（数据获取、订阅、DOM 操作）
useRef       → 持久化可变引用（不触发重渲染）
useMemo      → 缓存计算结果（避免重复计算）
useCallback  → 缓存函数引用（避免子组件重渲染）
useContext   → 跨组件共享状态（替代 props 逐层传递）
useReducer   → 复杂状态逻辑（替代 useState）

useState vs useRef：
const [count, setCount] = useState(0)   // 修改 → 触发重渲染
const timerRef = useRef(null)           // 修改 → 不触发重渲染

useEffect 依赖陷阱：
useEffect(() => {
  fetchData()
}, [])            // [] = 只执行一次（mount 时）
                  // [userId] = userId 变化时执行
                  // 无第二个参数 = 每次渲染都执行 ← 性能炸弹！

useMemo vs useCallback：
const sorted = useMemo(() => items.sort(), [items])     // 缓存值
const handleClick = useCallback(() => onClick(id), [id]) // 缓存函数
// 两者都是性能优化，不要滥用——简单场景不需要

闭包陷阱（面试常考）：
useEffect(() => {
  const timer = setInterval(() => {
    console.log(count)  // ❌ 永远是 0（闭包捕获了旧值）
  }, 1000)
  return () => clearInterval(timer)
}, [])  // 依赖数组为空 → count 不会更新

// ✅ 修复方式 1：加入依赖
}, [count])

// ✅ 修复方式 2：用 useRef
const countRef = useRef(count)
countRef.current = count   // 每次渲染更新 ref
```

**在本项目中：**
```jsx
// 面试页面的 Hooks 使用
function InterviewPage() {
  const [messages, setMessages] = useState([])      // 消息列表
  const [isStreaming, setIsStreaming] = useState(false) // 流式状态
  const scrollRef = useRef(null)                     // 滚动容器引用

  useEffect(() => {
    scrollRef.current?.scrollTo(0, 9999)             // 新消息自动滚底
  }, [messages])

  const { user } = useContext(AuthContext)            // 全局认证状态

  const handleSubmit = useCallback(async (answer) => {
    setIsStreaming(true)
    // ... SSE 流式接收
    setIsStreaming(false)
  }, [sessionId])                                    // 仅 sessionId 变化时重建
}
```

**面试记忆点：**
- Hooks 规则：只在**顶层**调用（不在 if/for 里），只在**函数组件或自定义 Hook** 中调用
- `useEffect` 返回的函数是**清理函数**——组件卸载或依赖变化时执行
- `useMemo` / `useCallback` 是优化手段，不是必需品——过早优化是万恶之源
- 面试常问：「useEffect 和 useLayoutEffect 的区别？」→ useEffect 异步（不阻塞渲染），useLayoutEffect 同步（DOM 更新后立即执行，适合测量布局）

---

# 十六、AI 工程进阶层

---

## Card 42 | Prompt Engineering（提示工程）

**一句话：** 设计结构化的指令文本，引导 LLM 产出高质量、可控的输出。

**核心概念：**
```
Prompt 设计五要素：
├── Role（角色）     → "你是一位资深 Python 面试官"
├── Task（任务）     → "针对候选人的回答进行评估"
├── Context（上下文）→ "候选人简历：...; 面试岗位：..."
├── Format（格式）   → "以 JSON 格式输出，包含 score 和 feedback"
└── Constraint（约束）→ "评分范围 1-10，反馈不超过 200 字"

常用技巧：
├── Few-shot       → 给 2-3 个示例，让模型学习输出模式
├── Chain-of-Thought (CoT) → "请一步步思考"，提升推理质量
├── System Prompt  → 设定全局角色和行为边界
├── 温度控制       → temperature=0（确定性输出）vs 0.7（创造性）
└── 结构化输出     → JSON Mode / Function Calling 保证格式

Prompt 注入防御：
├── 用户输入与系统指令分层（System vs User message）
├── 输入净化：过滤 "ignore previous instructions" 等攻击
├── 输出校验：验证 LLM 返回的格式和内容
└── 最小权限：LLM 不暴露内部 prompt

好 Prompt vs 坏 Prompt：
❌ "帮我出面试题"
✅ "你是一位 Python 高级面试官。根据以下简历信息，
    针对 FastAPI 和异步编程方向，出 3 道由浅入深的技术题。
    每道题包含：题目文本、考察要点、参考答案要点。
    以 JSON 数组格式返回。"
```

**在本项目中：**
```python
# 面试官 System Prompt（LangGraph 节点）
INTERVIEWER_SYSTEM = """
你是 TechSpar 的 AI 面试官，专注于 {job_title} 方向。
当前面试阶段：{phase}
候选人简历摘要：{resume_context}

面试规则：
1. 每次只问一个问题
2. 根据回答质量决定：追问 / 下一题 / 进入总结
3. 追问不超过 2 次
4. 语气专业但友好

输出格式：直接输出面试官的话，不要加角色标签。
"""

# RAG 增强 Prompt
QUESTION_WITH_CONTEXT = """
基于以下知识库内容，生成一道面试题：
---知识库---
{retrieved_context}
---END---
要求：题目应覆盖以上知识点，难度为{difficulty}级别。
"""
```

**面试记忆点：**
- Prompt Engineering 不是"调参数"，是**需求分析 + 结构化表达**
- System Prompt 设定角色边界，User Prompt 传递具体任务——两者分离是安全最佳实践
- 温度（temperature）：0 = 确定性最强（适合评估/判断），0.7-1.0 = 更有创造性（适合出题/对话）
- 面试回答时要强调：我们用 RAG 把知识库内容注入 prompt，而不是让 LLM 靠"记忆"回答

---

## Card 43 | WebSocket vs SSE 深度对比

**一句话：** 两种服务端推送技术——WebSocket 双向全双工，SSE 单向推送。

**核心概念：**
```
对比表：
┌──────────────┬──────────────────┬──────────────────┐
│              │    WebSocket     │      SSE         │
├──────────────┼──────────────────┼──────────────────┤
│ 协议         │ ws:// (独立协议)  │ HTTP/1.1 长连接   │
│ 方向         │ 双向（全双工）    │ 单向（服务端→客户端）│
│ 握手         │ HTTP → 101 升级  │ 普通 HTTP GET     │
│ 数据格式     │ 二进制 + 文本     │ 纯文本（UTF-8）   │
│ 断线重连     │ 需手动实现        │ 浏览器自动重连     │
│ 跨域         │ 需额外处理        │ 遵循 CORS         │
│ 代理穿透     │ 需 Nginx 特殊配置 │ 天然 HTTP 兼容    │
│ 并发连接     │ 可达 65535/服务器  │ 浏览器限 6 连接/域│
│ 适用场景     │ 聊天室/游戏/协同  │ 通知/流式输出/仪表盘│
└──────────────┴──────────────────┴──────────────────┘

SSE 选型理由（LLM 流式输出场景）：
├── LLM 输出是单向流（服务端 → 客户端），不需要双向
├── SSE 基于 HTTP，无需协议升级，部署更简单
├── 浏览器原生 EventSource API，无需额外库
├── Nginx 反代零配置（HTTP 长连接天然支持）
└── OpenAI API 本身也用 SSE 做流式输出——行业标准

WebSocket 适用场景：
├── 实时聊天（双向消息）
├── 多人协同编辑（双向状态同步）
├── 在线游戏（低延迟双向通信）
└── 实时数据推送 + 客户端指令（需双向）

SSE 连接生命周期：
客户端                              服务端
  │                                   │
  │── GET /api/stream ───────────────→│
  │   Accept: text/event-stream       │
  │                                   │
  │←── 200 OK ────────────────────────│
  │    Content-Type: text/event-stream│
  │                                   │
  │←── data: {"token": "Hello"} ──────│
  │←── data: {"token": " World"} ─────│
  │←── data: [DONE] ──────────────────│
  │                                   │
  │── 连接关闭 ───────────────────────│
```

**在本项目中：**
```python
# 后端 SSE（FastAPI StreamingResponse）
async def interview_stream(session_id: str):
    async def event_generator():
        async for chunk in llm.astream(messages):
            yield f"data: {json.dumps({'token': chunk})}\n\n"
        yield "data: [DONE]\n\n"
    return StreamingResponse(event_generator(),
                             media_type="text/event-stream")

# 前端 SSE（EventSource API）
const source = new EventSource(`/api/interview/${id}/stream`)
source.onmessage = (event) => {
    if (event.data === "[DONE]") {
        source.close()
        return
    }
    const { token } = JSON.parse(event.data)
    setMessages(prev => appendToken(prev, token))
}
```

**面试记忆点：**
- 选 SSE 还是 WebSocket？**单向推送选 SSE，双向通信选 WebSocket**
- SSE 的 HTTP 状态码是 **200**（不是 101——101 是 WebSocket 的协议升级）
- SSE 浏览器每个域名最多 6 个并发连接（HTTP/1.1 限制），HTTP/2 下无此限制
- 面试追问：「如果要加实时语音面试功能，用什么？」→ WebSocket（双向音频流）

---

# 十七、高可用与防护层

---

## Card 44 | 限流与降级（Rate Limiting & Circuit Breaker）

**一句话：** 保护系统在高并发或依赖故障时不被压垮的两道防线。

**核心概念：**
```
限流（Rate Limiting）—— 控制请求速率：

算法：
├── 固定窗口    → 每分钟最多 100 次（简单，但窗口边界可能突破 2x）
├── 滑动窗口    → 实时统计最近 60 秒内请求数（更精确）
├── 令牌桶      → 桶中有 N 个令牌，每次请求消耗 1 个，定速补充
│                 允许短时突发（桶满时），平稳限速（桶空时拒绝）
└── 漏桶        → 请求排队，以固定速率处理（严格匀速，无突发）

限流维度：
├── 全局限流    → 整个 API 总 QPS 上限
├── 用户限流    → 每个用户/Token 独立限额
├── IP 限流     → 防 DDoS / 暴力破解
└── 接口限流    → 不同接口不同限额（LLM 接口更严格）

---

降级与熔断（Circuit Breaker）—— 依赖故障时自保：

三种状态：
CLOSED（正常）→ 错误率超阈值 → OPEN（熔断，快速失败）
                                    ↓ 超时后
                              HALF-OPEN（放行少量请求探测）
                                ↓ 成功 → CLOSED
                                ↓ 失败 → OPEN

降级策略：
├── 返回缓存/默认值  → LLM 挂了返回预设回答
├── 功能降级        → 关闭 AI 追问，只用预设题库
├── 限流降级        → 超过容量排队等待
└── 告知用户        → "服务暂时繁忙，请稍后重试"
```

**在本项目中：**
```python
# 限流中间件示例（SlowAPI / 自定义）
from slowapi import Limiter
limiter = Limiter(key_func=get_remote_address)

@app.post("/api/interview/start")
@limiter.limit("5/minute")          # 每用户每分钟最多 5 次
async def start_interview(...):
    ...

@app.post("/api/ai-assistant/query")
@limiter.limit("20/minute")         # AI 查询限流更严（LLM 成本高）
async def ai_query(...):
    ...

# 降级策略
async def ask_llm_with_fallback(prompt):
    try:
        return await llm.ainvoke(prompt)       # 正常调 LLM
    except (TimeoutError, APIError):
        return FALLBACK_RESPONSE               # 降级：返回预设回答
```

**面试记忆点：**
- 限流四算法中**令牌桶**最常用——允许突发但总量可控
- 熔断器三状态：Closed → Open → Half-Open，像电路保险丝
- LLM API 调用**必须限流**——费用高 + 第三方 API 有自身限额
- 面试常问：「系统怎么应对流量突增？」→ 限流 + 降级 + 缓存 + 弹性扩容

---

## Card 45 | 日志与可观测性（Observability）

**一句话：** 通过日志、指标、链路追踪三大支柱，让系统运行状态可见、可查、可预警。

**核心概念：**
```
可观测性三支柱：

1. Logging（日志）→ 离散事件记录
   ├── 级别：DEBUG → INFO → WARNING → ERROR → CRITICAL
   ├── 结构化日志（JSON 格式，便于搜索和聚合）
   ├── 关键信息：时间、级别、模块、request_id、用户ID、耗时
   └── 不要记敏感信息（密码、Token、身份证号）

2. Metrics（指标）→ 聚合数值，趋势分析
   ├── 四大黄金指标（Google SRE）：
   │   ├── 延迟（Latency）     → P50/P95/P99 响应时间
   │   ├── 流量（Traffic）      → QPS / 并发数
   │   ├── 错误率（Errors）     → 5xx 比例
   │   └── 饱和度（Saturation） → CPU/内存/连接池使用率
   └── 工具：Prometheus + Grafana

3. Tracing（链路追踪）→ 单次请求的完整路径
   请求进入 → Nginx → FastAPI → LangGraph → LLM API → DB
   每一跳记录耗时，快速定位瓶颈
   └── 工具：OpenTelemetry / Jaeger

告警策略：
├── 错误率 > 5% 持续 5 分钟     → 告警
├── P99 延迟 > 3s 持续 10 分钟  → 告警
├── LLM API 连续失败 3 次       → 触发熔断 + 告警
└── 磁盘使用 > 80%              → 告警
```

**在本项目中：**
```python
# Python logging 配置
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s"
)
logger = logging.getLogger("techspar")

# 请求日志中间件
@app.middleware("http")
async def log_requests(request: Request, call_next):
    start = time.time()
    response = await call_next(request)
    duration = time.time() - start
    logger.info(
        f"{request.method} {request.url.path} "
        f"→ {response.status_code} [{duration:.3f}s]"
    )
    return response

# 关键业务日志
logger.info(f"面试开始 session={session_id} user={user_id}")
logger.error(f"LLM 调用失败 session={session_id}", exc_info=True)
logger.warning(f"LLM 响应超时 session={session_id} elapsed={elapsed}s")
```

**面试记忆点：**
- 可观测性三支柱：**Logging + Metrics + Tracing**——不同维度互补
- 日志级别选择：正常流程用 INFO，可恢复异常用 WARNING，需要人工处理的用 ERROR
- **结构化日志**（JSON）比纯文本日志更利于 ELK/Loki 等工具检索分析
- 面试常问：「线上出了问题怎么排查？」→ 看告警 → 查 Metrics 趋势 → 找 Trace 定位慢节点 → 看 Log 具体错误
