# 知识训练场实施计划

## 0. 本轮落地与建议调整

本轮已按 Phase 1 优先实现可验证的 MVP，并对原计划做两点收敛：

- 已新增独立后端能力：`backend/knowledge_training.py`、`backend/routers/knowledge_training.py`、`backend/prompts/knowledge_training.py`，不调用专项训练评估链路，不创建训练 session，不写画像。
- 已新增前端入口：`frontend/src/pages/KnowledgeTraining.tsx`、`frontend/src/api/knowledgeTraining.ts`，并在 `/knowledge-training` 和侧栏「训练」分组注册。
- `GET /api/knowledge-training/availability` 从“可选接口”调整为 MVP 必要接口。原因是页面需要一次性展示 file/chunk 状态和空知识库提示，避免前端拼多个知识库接口。
- MVP 支持「随机知识点」和轻量「高频考点」抽样；「薄弱点关联」与「到期复习」仍放在 Phase 2/3。原因是这两项会引入画像和间隔复习副作用，应等训练记录/熟悉度持久化设计稳定后再接入。
- 熟悉度标记已按计划只保存在前端 localStorage，不回写 profile weak points，也不进入历史训练报告。
- 后端补充了核心单测：section 切分、seed 抽样可复现、LLM JSON 归一化与来源兜底。

建议后续优先级：先观察 MVP 卡片质量与来源忠实度，再决定是否进入「薄弱点关联 / 到期复习 / 训练记录」闭环；不要过早把「不会」写回画像，否则会把一次记忆训练中的主观标记误当作正式评估结果。

## 1. 目标定位

新增一个「知识训练场」页面，让用户从已有知识库模块中抽取随机知识点，用卡片方式进行记忆训练。

它和现有「专项训练 / 问答演练」的边界必须清晰：

| 能力 | 专项训练 | 问答演练场 | 知识训练场 |
|---|---|---|---|
| 核心目标 | 出题、作答、评估 | 自由问答、总结沉淀 | 直接记忆知识点 |
| 用户输入 | 答案 | 对话问题 | 可选：只做熟悉度标记 |
| 系统反馈 | 分数、薄弱点、复习计划 | 对话回答、知识卡片 | 知识、例子、回忆问题、隐藏答案 |
| 是否评分 | 是 | 否 | 否 |
| 是否写入知识库 | 评估后可沉淀 | 用户确认后收录 | 默认不写入，只读取和训练 |

本功能的 MVP 不改变现有出题链路，不复用 drill 的评分与画像更新；只复用知识库、Topic、RAG、SSE、前端 UI 基础设施。

## 2. 当前代码依据

当前项目已经具备支撑该功能的基础能力：

- Topic 模块管理：`backend/routers/profile.py:23` 提供 `GET /api/topics`，`backend/routers/profile.py:28` 提供新增模块，`backend/routers/profile.py:59` 提供删除模块。
- 知识库读取与维护：`backend/routers/knowledge.py:92` 读取某模块核心知识文件，`backend/routers/knowledge.py:177` 上传 `.md`，`backend/routers/knowledge.py:222` AI 生成基础知识，`backend/routers/knowledge.py:430` 返回各模块 chunk 数。
- RAG 检索能力：`backend/graphs/rag_retrieval.py:57` 已有 topic 内多路检索、RRF 融合、语义去重、重排和指标统计。
- 专项训练流水线：`backend/graphs/drill_pipeline.py:87` 已经有分阶段 SSE、题目增量生成、JSON 修复、持久化等模式，但目标是出题，不应直接套用为知识训练。
- 前端路由与导航：`frontend/src/App.tsx:113` 已注册知识库页面，`frontend/src/App.tsx:121` 已注册问答演练场；`frontend/src/components/Sidebar.tsx:30` 定义导航分组，训练组已有 `问答演练场`，工具组已有 `知识库`。
- 前端知识库 API：`frontend/src/api/interview.ts:27` 拉取 topics，`frontend/src/api/interview.ts:328` 读取知识文件，`frontend/src/api/interview.ts:372` 调用 AI 生成知识库内容，`frontend/src/api/interview.ts:502` 查询 chunk 统计。
- 问答演练卡片沉淀：`frontend/src/api/qa_arena.ts:148` 已有总结生成，`frontend/src/api/qa_arena.ts:184` 已有收录知识库接口，可作为“人在环确认后写入”的参考，但知识训练场 MVP 默认不写入。

## 3. 产品方案

页面名称：知识训练场  
推荐路由：`/knowledge-training`  
推荐导航位置：侧栏「训练」分组，放在「问答演练场」之后。

### 3.1 核心流程

1. 用户进入知识训练场。
2. 页面加载所有知识库模块，并显示每个模块的文件数 / chunk 数。
3. 用户选择一个模块，例如 Python、Java、Agent。
4. 用户选择训练配置：
   - 抽取数量：3 / 5 / 10。
   - 抽取模式：随机知识点 / 高频考点 / 薄弱点关联 / 到期复习关联。
   - 深度：基础记忆 / 例子理解 / 面试表达。
5. 点击开始训练。
6. 后端从该模块知识库中抽取知识片段，生成训练卡片。
7. 前端按卡片展示：
   - 知识点标题。
   - 需要记忆的核心知识。
   - 例子或类比。
   - 自测问题。
   - 答案默认隐藏，点击后展示。
   - 来源文件 / 标题路径。
8. 用户可标记：已掌握 / 模糊 / 不会。
9. MVP 阶段熟悉度只保存在本页状态或 localStorage；后续再接入长期画像和间隔复习。

### 3.2 单张卡片结构

```json
{
  "id": "string",
  "topic": "python",
  "title": "GIL 的作用与边界",
  "knowledge": "需要直接记忆的核心知识，控制在 3-6 条要点内。",
  "example": "结合一个具体场景解释该知识点。",
  "question": "不看答案时用于主动回忆的问题。",
  "answer": "完整但精炼的参考答案，前端默认隐藏。",
  "tags": ["并发", "解释器"],
  "source_refs": [
    {
      "filename": "README.md",
      "header_path": "Python > 并发模型"
    }
  ]
}
```

### 3.3 关键交互要求

- 答案区域默认折叠，按钮文案为「查看答案」；展开后可切回「隐藏答案」。
- 卡片生成后先展示知识和例子，再展示自测问题，答案不自动露出。
- 不提供输入框强制用户作答，避免变成出题训练。
- 不展示分数，不触发 drill 的评估、画像更新和知识自进化。
- 每次「换一组」应重新抽样，避免总是同一批知识点。
- 空知识库或索引未完成时给出明确状态：可提示去知识库上传文件或重建索引。

## 4. 后端设计

### 4.1 新增模块

新增文件：

- `backend/routers/knowledge_training.py`
- `backend/knowledge_training.py`
- `backend/prompts/knowledge_training.py`

在 `backend/main.py` 中注册新 router。

### 4.2 API 设计

#### `POST /api/knowledge-training/cards`

生成一批知识训练卡片。建议使用 SSE，和现有 `fetchSSE` 体系保持一致。

请求：

```json
{
  "topic": "python",
  "count": 5,
  "mode": "random",
  "depth": "understand",
  "seed": "optional-client-seed"
}
```

字段约束：

- `topic`：必须存在于当前用户 `topics.json`。
- `count`：限制在 1-10，默认 5。
- `mode`：`random | high_freq | weak_points | due_review`。
- `depth`：`basic | understand | interview_expression`。
- `seed`：可选，用于复现同一批随机抽样。

SSE 事件：

```json
{"type": "progress", "message": "正在抽取知识片段..."}
{"type": "card", "data": { "...": "KnowledgeTrainingCard" }}
{"type": "done", "total": 5}
{"type": "error", "message": "模块不存在"}
```

#### `GET /api/knowledge-training/availability`

可选接口，用于页面初始化展示模块状态。

返回：

```json
{
  "topics": {
    "python": {
      "name": "Python",
      "file_count": 3,
      "chunk_count": 42,
      "available": true
    }
  }
}
```

如果为了减少接口数量，MVP 可以直接复用 `GET /api/topics` 和 `GET /api/knowledge/chunk-counts`。

### 4.3 抽样策略

「随机知识点」不要只靠向量检索，因为向量库通常按查询召回，不天然支持随机抽样。建议优先直接从用户知识文件中抽取结构化片段：

1. 读取 topic 对应目录中的 `.md / .txt / .py`。
2. Markdown 按 `##` / `###` 标题切分为 section。
3. 过滤太短、重复、明显空壳的 section。
4. 用 `seed + topic + timestamp` 洗牌抽样。
5. 每个 section 保留 `filename`、`header_path`、`content`。
6. 如果 section 太少，再用已有 RAG 检索补充上下文。

这种做法更符合“从已有知识库随机生成知识点”的需求，也不会把知识训练场绑定到出题用的 weak point 查询。

### 4.4 LLM 生成策略

新增 prompt，要求模型把抽中的原始片段改写为记忆卡片，而不是生成面试题。

核心约束：

- 只使用给定知识片段，不编造超出来源的事实。
- 每张卡片必须包含：知识、例子、问题、答案。
- 问题用于主动回忆，不是考试题，不需要评分标准。
- 答案要能独立解释问题，但避免过长。
- 返回严格 JSON 数组。

后端可复用 `backend.graphs.topic_drill._parse_json_response` 和 `backend.utils.stream_parser.extract_complete_objects` 的容错思路，支持增量 card 事件。

### 4.5 是否持久化

MVP 不新增数据库表，生成卡片不落库，只在前端会话态中使用。

第二阶段再新增持久化：

- `knowledge_training_sessions`
- `knowledge_training_cards`
- `knowledge_training_reviews`

持久化后才能支持：

- 今日训练记录。
- 知识点熟悉度趋势。
- 和 `spaced_repetition.py` 联动。
- 从「模糊 / 不会」反向写入 profile weak points。

## 5. 前端设计

### 5.1 新增文件

- `frontend/src/pages/KnowledgeTraining.tsx`
- `frontend/src/api/knowledgeTraining.ts`

修改：

- `frontend/src/App.tsx`：lazy import 并注册 `/knowledge-training`。
- `frontend/src/components/Sidebar.tsx`：训练分组新增「知识训练场」。

### 5.2 页面布局

页面保持工作台式布局，不做营销页。

推荐结构：

- 顶部工具栏：模块选择、抽取数量、模式、深度、开始训练按钮。
- 左侧或上方状态条：当前模块、文件数、chunk 数、生成状态。
- 主区域：训练卡片。
- 右侧或底部：本轮卡片列表和掌握标记统计。

单卡片 UI：

- 标题区：知识点标题、标签、来源。
- 知识区：3-6 条核心要点。
- 例子区：具体例子或类比。
- 回忆区：自测问题。
- 答案区：默认隐藏，点击按钮后展示。
- 操作区：上一张、下一张、已掌握、模糊、不会、换一组。

### 5.3 前端状态

建议状态模型：

```ts
interface TrainingCard {
  id: string;
  topic: string;
  title: string;
  knowledge: string;
  example: string;
  question: string;
  answer: string;
  tags: string[];
  source_refs: { filename: string; header_path?: string }[];
}

type Familiarity = "known" | "uncertain" | "unknown";
```

答案展示状态不要写入后端，只放在前端：

```ts
const [revealedAnswers, setRevealedAnswers] = useState<Record<string, boolean>>({});
const [familiarity, setFamiliarity] = useState<Record<string, Familiarity>>({});
```

## 6. 与现有能力的关系

### 6.1 可复用

- `getTopics`：获取知识库模块。
- `getChunkCounts`：展示模块是否已有索引。
- `load_topics`：后端校验 topic。
- 知识库目录规则：沿用 `settings.user_knowledge_path(user_id)`。
- SSE 工具：沿用 `streaming_response`、`sse_event`、前端 `fetchSSE`。
- JSON 解析容错：参考 drill pipeline。

### 6.2 不直接复用

- 不直接调用 `DrillPipeline`，因为它会生成题目、创建训练 session，并服务于评分闭环。
- 不调用 `endInterview` 或 `stream_evaluate_drill_answers`。
- 不把训练卡片写入 `sessions` 表，避免污染历史训练报告。
- 不自动沉淀到知识库，避免模型改写内容反向污染原始知识库。

## 7. 迭代拆分

### Phase 1：MVP

- 新增 `/knowledge-training` 页面和侧栏入口。
- 支持选择知识库模块。
- 支持随机抽取 3 / 5 / 10 张训练卡片。
- 每张卡片包含知识、例子、问题、隐藏答案。
- 支持上一张 / 下一张 / 查看答案 / 隐藏答案。
- 不做评分、不写入画像、不创建历史记录。

### Phase 2：训练体验增强

- 增加「高频考点」模式，读取 `high_freq` 内容辅助抽样。
- 增加「薄弱点关联」模式，结合用户 profile weak points 检索相关知识。
- 增加「到期复习」模式，结合 `spaced_repetition.py`。
- 本轮熟悉度保存到 localStorage，刷新后可恢复。

### Phase 3：长期记忆闭环

- 新增训练记录表。
- 把「模糊 / 不会」转换为轻量 weak point。
- 将「已掌握」接入间隔重复状态。
- 在成长报告中展示知识训练次数、掌握率、常错知识点。

### Phase 4：质量与评测

- 加入卡片 schema 校验和来源引用校验。
- 增加后端单测：topic 校验、section 切分、随机抽样、JSON 解析失败兜底。
- 增加前端测试：答案默认隐藏、点击后展示、切换卡片不串状态。
- 增加一组人工样例，检查卡片是否真的来自知识库而非泛化编造。

## 8. 验收标准

MVP 完成时必须满足：

1. 侧栏训练分组出现「知识训练场」入口。
2. `/knowledge-training` 能正常访问，未登录时仍受现有鉴权保护。
3. 用户可以选择不同知识库模块。
4. 用户可以生成随机知识训练卡片。
5. 每张卡片都有知识、例子、问题、答案。
6. 答案默认不可见，点击后展示，再点击可隐藏。
7. 不产生面试评分、不进入专项训练评估、不污染训练历史。
8. 空知识库、模块不存在、LLM 失败时有明确错误状态。
9. 桌面和移动端布局不重叠，按钮文字不溢出。
10. 前端类型检查和后端核心单测通过。

## 9. 风险与处理

| 风险 | 影响 | 处理 |
|---|---|---|
| LLM 编造来源外知识 | 用户记忆错误内容 | prompt 限制只使用来源片段，卡片带 source_refs，后端做来源非空校验 |
| 大文件读取过慢 | 页面生成等待长 | section 抽样放到 `asyncio.to_thread`，限制单次候选 section 数 |
| JSON 流式解析失败 | 卡片生成中断 | 复用现有 JSON 容错与 complete object salvage |
| 用户误以为这是考试 | 产品认知混乱 | 页面不提供答案输入、不评分、不展示正确率 |
| 与知识库编辑页职责重叠 | 页面复杂度上升 | 知识训练场只读知识库，编辑仍回到「知识库」页面 |

## 10. 推荐实施顺序

1. 后端先实现 section 抽样和静态假数据返回，打通接口。
2. 前端完成页面、路由、导航和答案隐藏/展示交互。
3. 接入 LLM 生成真实卡片。
4. 加入错误态、空态、加载态和移动端适配。
5. 补测试和文档说明。
6. 再决定是否进入 Phase 2 的高频、薄弱点、到期复习模式。
