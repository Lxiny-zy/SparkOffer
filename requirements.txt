# LLM Frameworks
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
# Persistent LangGraph checkpointer (resume-interview state survives restarts /
# multiple workers). Provides langgraph.checkpoint.sqlite.SqliteSaver.
langgraph-checkpoint-sqlite>=2.0.0
# Token counting for context budgeting (context_assembler.py). Already pulled in
# transitively by langchain-openai; pinned here so the budgeter never silently
# falls back to the char heuristic.
tiktoken>=0.7.0

# LlamaIndex
llama-index>=0.11.0
llama-index-readers-file>=0.4.0
llama-index-llms-openai>=0.3.0
llama-index-llms-openai-like>=0.2.0
llama-index-embeddings-openai>=0.3.0

# Optional local embedding dependencies live in requirements.local-embedding.txt

# PDF reading (for resume)
pypdf>=4.0.0

# Web
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12

# Storage
aiosqlite>=0.20.0

# Speech-to-text (Qiniu OSS + DashScope cloud ASR)
qiniu>=7.12.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# HTTP (with SOCKS5 proxy support)
httpx[socks]>=0.26.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0

# Cache (optional — backend auto-fallbacks to in-memory LRU if Redis unreachable)
redis>=5.0.0

# Vector store (optional — only when VECTOR_BACKEND=qdrant; the default numpy
# backend needs nothing extra). llama-index-vector-stores-qdrant 用于知识库
# RAG 索引切到 Qdrant 的可选路径。
qdrant-client>=1.12.0
llama-index-vector-stores-qdrant>=0.3.0
